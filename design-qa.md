**Findings**
- [P1] Project color system was previously missing and is now partially fixed
  Location: `src/main.ts`, `src/styles.css`, detail/work states.
  Evidence: reference projects carry `color.bg`, `color.txt`, and `color.work` through WebGL clear color, image tint, title color, pagination, and work media. Local evidence now uses the live reference project color token list; `/nian-nian-002` resolves to the reference second-project palette with Detail/Work `--surface-rgb: 190, 190, 190`, `--text-rgb: 30, 30, 30`, and Work `--work-rgb: 168, 168, 168`.
  Impact: this is a core part of the reference image exhibition effect, not decorative theming.
  Fix: continue tuning per-photo palettes against live reference states and ensure every project transition carries color through GL, DOM, and controls.

- [P1] Some reference motion primitives are still approximated rather than 1:1
  Location: `src/main.ts` WebGL layout, pagination loop, title letter motion, work media rail.
  Evidence: reference source uses `R.Damp`, mode-specific lerps, raw WebGL plane transforms, `pgnX` number motion, title class `L`, `letter.move({ start: "home", end: "work", d: 1200 })`, and work media `lT3d`/`sT3d`/`aY` tracks. Local now ports major pieces of that stack: reference-like Home/Detail wheel paths, pixel-rectangle hover checks, Detail previous meta/copy out layers, WebGL `x.targ - x.curr` plane offsets, clipped title rows/glyph wrappers, dynamic per-photo title density, Work `PROJECTS` timing, Detail top Project control geometry, Detail Explore geometry, reference-like `uUvScale`, mode-specific plane opacity/tinting, Detail side-plane clicks, top pagination metrics, Work rail/media DOM motion, Work image reveal timing, static Work stage wrapper, and Work wheel behavior. Detail switching and Work media switching are improved but still not proven exact.
  Latest v50 evidence: Work media switches now only mutate the outgoing and incoming large-media layers, matching reference `class b.up(i)` instead of mass-repositioning non-participating `lT3d` entries.
  Latest v51 evidence: `/tmp/compare-v51-work-stable.jpg` and `/tmp/compare-v51-work-entry-300ms.jpg` place the live reference and local Work states side by side at 1440 x 900. Both live reference and local stable Work now report active-frame opacity `0.7`, active-frame top near `151.6px`, right offset `44px`, and a `92 x 57` frame, correcting the previous local `0.95` line weight. The Work-entry 300ms sample also now reports local active-frame opacity `0.7`, matching the reference `#w-a` behavior where the frame is already at full opacity while its Y position is still moving. After the entry-only DOM lerp pass, 300ms motion is close: reference active frame top `310.38px` vs local `311.15px` (`+0.77px`), and reference active large layer top `356.36px` vs local `356.02px` (`-0.34px`). The remaining measured miss in that same state is image reveal opacity: reference active image opacity `0.306035`, local `0.216532` (`-0.09`). The same pass still shows broader remaining differences in content palette, title mass, and project/media slot structure, so full visual parity remains blocked.
  Latest v52 evidence: Work-entry image reveal timing is now aligned against the live v51 reference sample. The reference Work-entry 300ms active image opacity was `0.295278` with active large-layer top `356.29px`; the new clean-profile local `scripts/qa-work-entry-opacity-v52.mjs` sample reports active image opacity `0.293103`, loaded `true`, and active large-layer top `362.60px` at 300ms, then stable opacity `1`. A second run after the same patch reported local opacity `0.306111` and layer top `356.26px`, showing the opacity now sits in the live reference band rather than the previous `0.216532`. Work internal switching still has an early fade instead of a blank frame: `scripts/qa-work-layer-fx-v19.mjs` reports switch 120ms active image opacity `0.0999086`, switch 300ms `0.522806`, stable `1`, with no failures. The broader full-site parity blockers remain unchanged.
  Latest v53 evidence: `/tmp/compare-v53-work-switch-120ms.jpg`, `/tmp/compare-v53-work-switch-300ms.jpg`, and `/tmp/compare-v53-work-switch-stable.jpg` compare live reference and local Work internal switching at 1440 x 900. The reference uses global media slots in Work (`workIndex 0 -> 2` for House of Gucci), and local now mirrors that slot model instead of compacting the active frame by media order. The latest normalized v53 report also distinguishes slot-distance differences from true motion drift: at 120ms, active large-media progress is reference `0.465` vs local `0.4891` (`+0.02`), and active-frame progress is reference `0.6477` vs local `0.6324` (`-0.02`). At 300ms, active large-media progress is reference `0.8404` vs local `0.8411` (`0.00`), active-frame progress is reference `0.9532` vs local `0.9097` (`-0.04`), active image opacity is reference `0.112443` vs local `0.112601`, active large top differs by `-0.63px`, exiting top by `-0.39px`, and active-frame top by `+0.22px`. Stable Work now aligns at active large top `206.36px` vs reference `206.37px`, active image opacity `1`, previous offscreen layer top `-624.19px` vs reference `-624.43px`, and active-frame top `271.5px` in both. The earlier `18px` 120ms active-frame absolute delta was a mismatched-start artifact, not evidence that the reference `.13` frame damping was wrong.
  Latest v54 evidence: `/tmp/compare-v54-detail-switch-before.jpg`, `/tmp/compare-v54-detail-switch-120ms.jpg`, `/tmp/compare-v54-detail-switch-650ms.jpg`, and `/tmp/compare-v54-detail-switch-stable.jpg` compare live reference and local Detail project switching at 1440 x 900. The Detail top Project control now aligns to the reference frame: reference `top 49 / left 689.44 / 61.13 x 79`, local `top 49 / left 689.5 / 61 x 77`. The Detail bottom Explore control now also aligns to the reference vertical frame: reference `top 747 / height 104`, local `top 747 / height 104`, with only width differing modestly (`52.25px` vs `54.42px`). Existing Detail switch WebGL/meta regressions still pass, and no runtime exceptions were reported. Remaining visible drift in the v54 comparisons is mostly title/content density, copy structure, per-photo palette/content mismatch, and typography mass rather than these two controls.
  Latest title-density evidence: the same v54 side-by-side comparisons now use dynamic local project titles instead of a fixed compact title. `/tmp/compare-v54-detail-switch-before.jpg` shows local `NIAN NIAN / DIARY` against the reference `HOUSE OF / GUCCI`, giving the Detail frame a comparable two-line title mass. The captured metrics show the reference before-state first line with 7 glyphs and local with 8 glyphs; after switching, the reference short title uses 3 glyphs and local now switches to the 3-glyph `JOY` title. In the 120ms switch frame, local `JOY` x positions `[143.41, 538.97, 961.81]` closely track the reference `[141.69, 535.88, 962.82]`. The v23 and v33 title regressions still pass with no runtime exceptions after this title-density change.
  Latest v55 evidence: the Detail bottom information layer now matches the live reference geometry more tightly. In `/tmp/compare-v54-detail-switch-before.jpg` and `/tmp/compare-v54-detail-switch-stable.jpg`, the left info grid now reports reference/local `top 805 / left 310 / width 410 / height 48`, correcting the previous local `top 801 / height 52`. The right copy block now uses reference-like shrink-to-fit text width rather than a fixed `250px`: stable reference `228.28px`, local `227.66px`; switch-120 reference `228.28px`, local `227.66px`; before reference `238.45px`, local `227.66px`. `scripts/qa-detail-meta-switch-v26.mjs` still reports no failures after the row-height and copy-width patch.
  Latest v56 evidence: the Detail/Work control geometry now matches the live reference more closely. The same v54 comparison script now reports the top Project control at reference `61.13 x 79` vs local `61 x 79`, fixing the previous local `61 x 77`. The bottom Explore control now measures reference `left 693.88 / width 52.25 / height 104` vs local `left 693.65 / width 52.70 / height 104`, reducing the previous width miss (`54.42px`) to `+0.45px`. `scripts/qa-project-control-v24.mjs` still reports the reference-like 1400ms/1200ms/1400ms child timings and no failures, and `scripts/qa-reference-hover-v20.mjs` verifies Home hover Enter and Detail side-plane click behavior with no runtime exceptions.
  Latest v57 evidence: `/tmp/compare-v57-control-project-symbol-hover-crop.jpg` and `/tmp/compare-v57-control-explore-root-hover-crop.jpg` now compare live reference/local hover states at 1440 x 900. The reference top Project symbol stays a cross on hover; local now matches by keeping the Project polygon unchanged. The reference bottom Explore control morphs from cross to downward arrow on root hover; local matches that behavior and `scripts/capture-control-hover-compare-v57.mjs` reports no failures or runtime exceptions. Follow-up regressions `scripts/qa-reference-hover-v20.mjs` and `scripts/qa-project-control-v24.mjs` both still pass.
  Latest v58 evidence: the Work active-frame lerp now mirrors reference `class b.up()` more closely by holding `.13` during media switches instead of damping back to `.08` immediately. A fresh live reference/local `scripts/capture-work-switch-compare-v53.mjs` run reports normalized active-frame progress at 120ms as reference `0.6477` vs local `0.6717` (`+0.02`), improving the previous local lag around `-0.05`; at 300ms, reference `0.9533` vs local `0.9562` (`0.00`). Active large-layer progress also remains close: 120ms `+0.02`, 300ms `+0.01`. Follow-up local regressions `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, and `scripts/qa-work-rail-v18.mjs` pass with no runtime exceptions.
  Latest v59 evidence: Detail project-switch title/info exits now use the reference `L.motion({ action: "hide", e: "o3", d: 500 })` feel more closely. The refreshed `/tmp/compare-v54-detail-switch-120ms.jpg` shows the local outgoing large title clipped into vertical strips instead of remaining as a full static overlay, matching the live reference switch frame more closely. The outgoing title keeps opacity at `1` and moves horizontally through its clipping wrappers; outgoing meta/copy rows now use the same fast-start out curve. Follow-up local regressions `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, and `scripts/qa-detail-plane-offset-v27.mjs` pass with no runtime exceptions.
  Latest v60 evidence: Home-to-Detail entry now has a live reference/local comparison harness in `scripts/capture-home-detail-entry-compare-v60.mjs`. The refreshed `/tmp/compare-v60-home-detail-120ms.jpg`, `/tmp/compare-v60-home-detail-300ms.jpg`, and `/tmp/compare-v60-home-detail-750ms.jpg` show the local title reveal holding later glyphs longer, closer to reference `L.title` staging. In the 300ms sample, the last local first-line glyph now sits at `-56.49px` versus the reference `-59.48px`, replacing the previous too-settled local value around `-36.90px`. The plane entry remains close: local `detailMix` is `0.4978` at 120ms, `0.8537` at 300ms, and `0.9871` at 750ms, with no runtime exceptions. Follow-up regressions `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, and `scripts/qa-plane-target-snap-v30.mjs` pass.
  Latest v62 evidence: Home wheel pagination now matches the reference c2d coordinate model instead of the earlier right-edge approximation. `scripts/capture-home-wheel-compare-v61.mjs` now reports live reference `workL: 30`, `dpr: 2`, `gapX: 16`, `outWidth: 1`, and `leftOut: 1200`; local keeps `photoCount: 17` but draws `count: 30` visual ticks with the same centered rail geometry. The refreshed `/tmp/compare-v61-home-wheel-before.jpg`, `/tmp/compare-v61-home-wheel-160ms.jpg`, and `/tmp/compare-v61-home-wheel-stable.jpg` show the local Home rail moved from the clipped top-right position into the reference top-center band. Stable alpha now matches exactly: reference `2700 / 137700`, local `2700 / 137700`; mid-wheel is also close, with reference `2742 / 139374` vs local `2784 / 141042` at 160ms. `scripts/qa-pagination-canvas-v16.mjs` also verifies Detail hover rail revival after the open-position hit-test fix. The broader full-site blocker remains because other motion/content surfaces still need live-frame parity, not because the Home rail is still clipped or underdrawn.
  Latest v63 evidence: Work internal large-image reveal timing is now aligned to the reference source instead of a single cache-dependent screenshot. Reference `class b.imgLoad()` sets `e = r ? 100 : 80 * s`, then runs `a.decode().then(... new R.Delay(..., e))`; `class b.up(i)` only calls `imgLoad(i, "l")`, so large media reveal is decode-completion plus `100ms`, not a fixed long switch delay. Local now uses `WORK_SWITCH_REVEAL_DELAY = 100` through the same decode-gated `motion.revealAt = performance.now() + delay` path. The refreshed `scripts/qa-work-layer-fx-v19.mjs` run reports switch-120 active image opacity `0`, switch-300 opacity `0.43678`, stable opacity `1`, with wrapper opacity staying `1` and no runtime exceptions. A fresh live reference/local `scripts/capture-work-switch-compare-v53.mjs` run reports 120ms active opacity `0` for both, 300ms active opacity reference `0.383913` vs local `0.332086`, normalized active-layer progress `0.8649` vs `0.8531` (`-0.01`), active-frame progress `0.9646` vs `0.9592` (`-0.01`), and stable active layer/frame alignment exactly at progress `1`.
  Latest v64 evidence: the WebGL background is now rendered by the shader stack instead of being approximated by CSS/clear color. Reference source has `planeBg` using the same shader as image planes with `n` set to `0`, while textured planes set `n` to `1`; the fragment shader outputs the global GL color when `n != 1`. Local now mirrors that with a persistent `backgroundPlane`, shared `uHasTexture` shader branch, and transparent renderer clear. `scripts/qa-webgl-background-v64.mjs` reports Detail and Work both have exactly two canvases (`#webgl` and `.pagination-canvas`), `backgroundPlane: "1"`, and WebGL background colors matching the shell surface palette: Detail `145, 197, 170`, Work `140, 195, 166`. Follow-up `qa-shader-light-v28`, `qa-plane-target-snap-v30`, and `qa-home-base-palette-v43` all pass with no runtime exceptions after this shader-architecture change.
  Latest v65 evidence: Home-to-Detail pagination DOM geometry now matches the live reference's narrow top band instead of a full-viewport overlay. The refreshed `scripts/capture-home-detail-entry-compare-v60.mjs` run reports reference/local pgn root rects both at `top 50`, `left 0`, `width 1440`, `height 14` for 120ms, 300ms, 750ms, and stable frames. Title first-entry staging is also closer to the reference `L.title` model: Detail active title reveal now uses `1800ms` and `--title-entry-delay-step: 40ms`, giving normalized residual-transform MAE of `0.024` at 120ms, `0.025` at 300ms, and `0.002` at 750ms across the comparable first-line glyphs. Verified with `npm run build`, `node --check src/main.js`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-pgn-v15.mjs`, `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-title-mode-move-v33.mjs`, and `scripts/qa-title-work-delay-v23.mjs`. Remaining drift is mostly content/title-string differences and the still-unfinished full 1:1 motion audit across all routes.
  Latest v66 evidence: mobile behavior now follows the live reference instead of forcing the desktop WebGL gallery onto narrow screens. At 390 x 844 DPR 2 with an iPhone user agent, the reference reports `canvasCount: 0` and shows the static dark page with `VISIT ON A DESKTOP FOR A FULL PORTFOLIO`; local now hides WebGL/pagination on mobile Home and Detail, shows a matching dark desktop prompt, and keeps `/studio` available by excluding `.gallery-shell.is-studio` from the fallback. `scripts/capture-mobile-fallback-compare-v66.mjs` generated `/tmp/compare-v66-mobile-home.jpg` and `/tmp/compare-v66-mobile-detail.jpg`, reported local mobile Home/Detail `fallbackVisible: true`, `visibleCanvasCount: 0`, local Studio `fallbackVisible: false`, `studioVisible: true`, and no runtime exceptions.
  Latest v67 evidence: Work internal media switching now matches the live reference image reveal timing much more closely. A fresh `scripts/capture-work-switch-compare-v53.mjs` run at 1440 x 900 previously showed the 300ms active large-image opacity at reference `0.0870006` vs local `0.358884` (`+0.27`) while layer/frame positions were already close. Local now keeps a `240ms` minimum Work-switch reveal gate to compensate for local assets decoding faster than the live reference path. The refreshed live comparison reports 300ms active opacity reference `0.124068` vs local `0.125048`, active-layer progress reference `0.8463` vs local `0.8405` (`-0.01`), frame progress reference `0.9561` vs local `0.9532`, stable active opacity `1` in both, and no runtime exceptions. `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, and `scripts/qa-work-layer-noninvolved-v50.mjs` all pass after the reveal-gate patch.
  Latest v68 evidence: Detail switch plane telemetry is now measured in explicit screen pixels on the local side instead of mixing WebGL world-space values with the reference site's internal `_A.h.pCurr/pTarg` objects. `src/main.js` now exposes active and exiting plane current/target rects through `#webgl.dataset`, and `scripts/capture-detail-switch-compare-v54.mjs` labels metric types as `reference-internal-pCurr-pTarg` versus `local-screen-px-dataset`. The refreshed v54 live comparison still reports close control geometry and no runtime exceptions; local stable screen rect after the switch is `left 324.86 / top 224.25 / 790.48 x 451.5`, settling onto target `left 324.76 / 790.5 x 451.5`. The earlier apparent `90px` Detail plane mismatch was a metric-unit mismatch, not a safe animation parameter target. Verification: `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, `npm run build`, `scripts/qa-detail-plane-offset-v27.mjs`, and refreshed `scripts/capture-detail-switch-compare-v54.mjs`.
  Latest v69 evidence: Home/base mode now matches the live reference's black exhibition entry surface instead of the previous white paper surface. `/tmp/compare-v60-home-detail-before.jpg` now shows both reference and local Home on a near-black background with subdued WebGL strips and top pagination, while Detail/Work still keep project palettes. `scripts/qa-home-base-palette-v43.mjs` reports Home rest and scrolled Home both at `paletteMode: base`, `--surface-rgb: 20, 20, 20`, `--text-rgb: 191, 150, 65`, and `--work-rgb: 214, 216, 210`; Detail `/nian-nian-002` still reports `paletteMode: detail` with project surface/text colors, and Work still reports `paletteMode: work` with project `--work-rgb`. Verification: `node --check src/main.js`, `node --check scripts/qa-home-base-palette-v43.mjs`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, `npm run build`, `scripts/qa-home-base-palette-v43.mjs`, `scripts/qa-webgl-background-v64.mjs`, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-home-wheel-v21.mjs`, and `scripts/qa-home-detail-keyboard-v13.mjs`.
  Latest v70 evidence: Home top pagination brightness is now calibrated against the live reference rather than only matching geometry. Pixel sampling on the refreshed `/tmp/ref-v60-home-detail-before.png` and `/tmp/local-v60-home-detail-before.png` reports pure background `20 / 20 / 20` for both; the top pagination band now measures reference luminance `24.21` and local `24.18` over the same `x 595..852 / y 46..66` crop. Local keeps the aligned pagination extent while reducing the previous too-bright Home canvas ticks with `HOME_PAGINATION_BASE_OPACITY = 0.15`. Verification: `node --check src/main.js`, `node --check scripts/qa-home-base-palette-v43.mjs`, `npm run build`, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-home-base-palette-v43.mjs`, `scripts/qa-home-pagination-wave-v29.mjs`, `scripts/qa-pagination-canvas-v16.mjs`, `scripts/qa-home-wheel-v21.mjs`, `scripts/qa-home-detail-keyboard-v13.mjs`, and `scripts/qa-webgl-background-v64.mjs`.
  Latest v71 evidence: Detail drag-to-Home exit now follows the live reference's target-carry timing instead of advancing the Home strip on the threshold-crossing event. The new live comparison harness `scripts/capture-detail-drag-compare-v71.mjs` generated `/tmp/compare-v71-detail-drag-before.jpg`, `/tmp/compare-v71-detail-drag-120ms.jpg`, `/tmp/compare-v71-detail-drag-260ms.jpg`, and `/tmp/compare-v71-detail-drag-settled.jpg`. In the 120ms sample, both reference and local remain in Detail with target scroll/index `0`; in the 260ms sample, both have switched to Home/out while the visual plane is collapsing toward the Home strip and Home target scroll still remains `0`; in the settled sample, reference active plane is `x 676.80 / y 309.86 / 86.41 x 280.27` against target `682.5 / 311.25 / 75 x 277.5`, while local is `left 677.55 / top 310.05 / 84.89 x 279.91` against target `682.5 / 311.25 / 75 x 277.5`. The old local behavior jumped to `targetScrollPx 192` at 120ms and `432` by 260ms; the new behavior keeps `targetScrollPx 0` until a later Home-mode drag event, matching the reference `modeOut()`/`xTargUp()` split. Verification: `node --check src/main.js`, `node --check scripts/capture-detail-drag-compare-v71.mjs`, refreshed `scripts/capture-detail-drag-compare-v71.mjs`, updated `scripts/qa-detail-drag-carry-v38.mjs`, `scripts/qa-home-drag-edge-v45.mjs`, `npm run build`, `scripts/qa-home-wheel-v21.mjs`, `scripts/qa-home-detail-keyboard-v13.mjs`, `scripts/qa-pagination-canvas-v16.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, and `scripts/qa-home-pagination-wave-v29.mjs`.
  Latest v72 evidence: Work large-layer background fade now follows the reference `class b.imgLoad()` / `.w-l-bg.fx` model instead of binding every large layer to a shared Work-stage background opacity. Local now keeps each `.work-layer-bg` at `--work-layer-bg-opacity: 1` until that layer's decoded image reaches its reveal gate, then applies the reference-like `opacity 100ms linear 1000ms` fade. `scripts/qa-work-layer-fx-v19.mjs` now verifies this directly: stable index 0 has active background opacity `0`; during a switch, incoming index 1 has background opacity `1` at 120ms and still computed `1` at the early fade sample, while the already-loaded exiting index 0 remains `0`, matching the reference behavior where the old `.w-l-bg.fx` is not re-lit. The refreshed `scripts/capture-work-switch-compare-v53.mjs` run reports Work-switch 120ms active large-layer progress reference `0.4448` vs local `0.4445`, active top `667.55px` vs `667.64px`, exiting top `-163.14px` vs `-162.81px`; at 300ms, active progress reference `0.8538` vs local `0.8411`, active top delta `+10.49px`, frame top delta `+2.06px`; stable active top differs by `0.02px` and frame top by `0px`. Verification: `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, `scripts/qa-work-color-v31.mjs`, `scripts/qa-work-thumb-reentry-v47.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, refreshed `scripts/capture-work-switch-compare-v53.mjs`, and `npm run build`.
  Latest v73 evidence: Home-to-Detail WebGL plane entry now matches the live reference's early/mid expansion more closely, and the comparison harness now records local active plane current/target screen rects instead of only `detailMix`. The harness also now uses the same Enter-or-click fallback as other reference scripts, fixing the intermittent reference Detail-entry timeout. Before the patch, `/tmp/compare-v60-home-detail-120ms.jpg` showed local active plane `left 497.62 / top 266.23 / 445.20 x 367.54` against reference `x 547.77 / y 276.59 / 360.05 x 346.82`, so local was visibly too expanded. After adding a short Home-to-Detail-only geometry easing window, the refreshed v60 report shows 120ms local `left 539.92 / top 276.50 / 360.74 x 347.00` against reference `x 533.94 / y 272.92 / 390.24 x 354.16`, and 300ms local `left 384.07 / top 238.65 / 672.03 x 422.69` against reference `x 381.90 / y 237.55 / 681.15 x 424.91`; stable settles to `left 324.78 / top 224.26 / 790.43 x 451.48` against the `324.75 / 224.25 / 790.5 x 451.5` target. Verification: `node --check src/main.js`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-detail-drag-carry-v38.mjs`, `scripts/qa-home-detail-keyboard-v13.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `npm run build`.
  Latest v74 evidence: Home-to-Detail title/pagination entry now has normalized telemetry in `scripts/capture-home-detail-entry-compare-v60.mjs`, so title glyph motion is compared by residual transform percentage instead of raw glyph pixel width. Adding an `18ms` Detail-entry base delay to the active title reveal reduced the first-line title residual MAE to `0.014` at 120ms, `0.0204` at 300ms, `0.0022` at 750ms, and `0.0001` stable in the refreshed live reference/local v60 capture. The same run reports the top pagination band still at reference/local `top 50 / width 1440 / height 14`; after delaying the show-out pagination entry to `150ms`, the 300ms pgn transform deltas are now `a -0.1601px` and `b +0.6319px`, with 750ms nearly settled at `a -0.0062px` and `b +0.024px`. `scripts/qa-pgn-home-entry-v44.mjs` now samples 120ms local pagination at `translate3d(95.55%, 0, 0)` / `translate3d(-95.55%, 0, 0)`, keeping the digits held offscreen like the live reference early frame. Verification: `node --check src/main.js`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, `scripts/qa-detail-drag-carry-v38.mjs`, `scripts/qa-home-detail-keyboard-v13.mjs`, and `npm run build`.
  Latest v75 evidence: Work internal media-switch image reveal now follows the reference source timing again: decode completion plus a `100ms` reveal gate instead of the previous local-only `240ms` hold. Two pre-patch live reference/local captures showed the 300ms active large-image opacity drifting from reference `0.384025` / `0.43524` to local `0.125315` / `0.125269`, while 120ms still needed to remain blank. After the patch, the refreshed `scripts/capture-work-switch-compare-v53.mjs` run reports 120ms active opacity `0` for both reference and local, and 300ms active opacity reference `0.383949` vs local `0.332178`, reducing the opacity miss from about `-0.31` to `-0.05`. The same refreshed run keeps the existing close position tracks: 120ms active-layer normalized delta `0`, 300ms active-layer delta `-0.01`, 300ms frame delta `-0.01`, and stable active/frame progress `1` for both. Verification: `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, refreshed `scripts/capture-work-switch-compare-v53.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, `scripts/qa-work-color-v31.mjs`, `scripts/qa-work-thumb-reentry-v47.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-keyboard-v12.mjs`, and `npm run build`.
  Latest v76 evidence: Home wheel and Home drag now have normalized live reference/local telemetry instead of comparing raw reference internals to local pixels. `scripts/capture-home-wheel-compare-v61.mjs` now reports reference wheel travel as `reference.x * reference.workL` and local wheel travel as `pgn.waveCenter`, plus matched latency as `reference.latencyX` versus `local.paginationLatencyLift / 30`. A fresh run reports wheel travel deltas of `0.015`, `0.032`, `0.047`, and `0.037` project widths at 80ms, 160ms, 320ms, and stable, with latency deltas `0`, `-0.0006`, `-0.0021`, and `-0.0001`; the canvas alpha span differs by only `0-1px` during motion. The new `scripts/capture-home-drag-compare-v76.mjs` compares Home drag target/current/latency on the same normalized index scale; at drag 200ms and settled, reference and local target index, current index, and latency are all exact zero-delta matches for the sampled drag path. This means Home wheel/drag should not be behavior-tuned from the older raw-field drift. Verification: `node --check scripts/capture-home-wheel-compare-v61.mjs`, `node --check scripts/capture-home-drag-compare-v76.mjs`, refreshed `scripts/capture-home-wheel-compare-v61.mjs`, `scripts/capture-home-drag-compare-v76.mjs`, `scripts/qa-home-wheel-v21.mjs`, `scripts/qa-home-pagination-wave-v29.mjs`, `scripts/qa-home-drag-edge-v45.mjs`, `scripts/qa-home-target-lag-v42.mjs`, `scripts/qa-detail-drag-carry-v38.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-entry-v11.mjs`, `scripts/qa-work-wheel-v17.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, `scripts/qa-work-thumb-reentry-v47.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-media-order-direction-v34.mjs`, `scripts/qa-title-work-delay-v23.mjs`, and `npm run build`.
  Latest v77 evidence: Detail project switching now compares the same interaction path on both sides. `scripts/capture-detail-switch-compare-v54.mjs` now drives local from Home into Detail before pressing ArrowRight, matching the reference path, and it reports reference plane screen rects after subtracting `_A.h.x.curr` so local screen-pixel datasets are no longer compared against raw `pCurr/pTarg` internals. Local Detail switching now uses the same short early geometry hold as Home-to-Detail for the first `180ms` of an in-Detail project change, and the local fallback title bank no longer repeats the first two titles, so the first Detail switch exercises the reference-like long-title to short-title choreography. The refreshed v54 run reports before-state plane screen alignment within about `0.5px`, 120ms current plane width reference/local `567.79px` vs `553.86px`, 650ms current plane `left 353.22 / width 778.36` vs local `left 358.83 / width 779.02`, and stable plane `left 324.81 / width 790.47` vs local `left 324.85 / width 790.47`, with no runtime exceptions. Title char placement at the switched-to short title remains close: reference `[141.68, 535.88, 962.81]`, local `[143.41, 538.96, 961.80]`. Verification: `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, refreshed `scripts/capture-detail-switch-compare-v54.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, `scripts/qa-detail-drag-carry-v38.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `npm run build`.
  Latest v78 evidence: Work media opacity now separates first Work entry from internal Work media switches and prevents the local WebGL plane from leaking an already-visible image under the DOM Work layer. `WORK_ENTRY_REVEAL_DELAY` is now `70ms`, `WORK_ENTRY_LERP` is `0.09`, the Work-entry image fade is `650ms`, and internal Work switches use a much later `WORK_SWITCH_REVEAL_DELAY = 600` with the default `3000ms` image fade. During Work internal switches where `workIndex !== activeIndex`, the active WebGL plane now targets alpha `0`; the refreshed v53 report shows stable local `activePlaneAlpha/Target: 0/0`. The final v53 live comparison reports 120ms active-layer progress reference/local `0.4136` vs `0.4186` (`+0.01`), 300ms active image opacity `0` for both, no visible image layer on either side at 300ms, and stable active top aligned at `206.27px` reference vs `206.26px` local. Work entry v52 now passes with 300ms image opacity `0.351999` and no runtime exceptions, while v19 verifies internal switch 120ms/300ms opacity stays `0` and v22 verifies already-loaded returns still skip the fade. Verification: `node --check src/main.js`, `node --check scripts/capture-work-switch-compare-v53.mjs`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-loaded-return-v22.mjs`, `node --check scripts/qa-work-reentry-fx-v46.mjs`, `node --check scripts/qa-work-entry-opacity-v52.mjs`, refreshed `scripts/capture-work-switch-compare-v53.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, `scripts/qa-work-thumb-reentry-v47.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, `scripts/qa-work-entry-opacity-v52.mjs`, and `npm run build`.
  Latest v79 evidence: Detail project-switch geometry now has its own early easing branch instead of sharing the Home-to-Detail entry hold. Home-to-Detail keeps the previously tuned `0.05` early geometry ease, while in-Detail project switches use `0.052` for the first `180ms`. The refreshed v54 live comparison reports 120ms plane width reference/local `568.67px` vs `568.28px`, correcting the previous too-narrow local early frame, while the local current plane still sits about `24.75px` to the right of the reference (`left 845.03` vs `869.78`), so this remains improved rather than fully exact. The same run reports 650ms plane `left 330.31 / width 788.13` vs local `left 330.05 / width 788.87`, and stable `left 324.75 / width 790.5` on both reference and local. Home-to-Detail was checked separately with v60 after the split: 300ms plane geometry remains close, 750ms and stable frames settle to the same target, and title/pagination telemetry remains in the prior tolerance band. Verification: `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, refreshed `scripts/capture-detail-switch-compare-v54.mjs`, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, `scripts/qa-detail-plane-offset-v27.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-fx-v19.mjs`, and `npm run build`.
  Latest v80 evidence: Detail/Work plane X now mirrors the reference's unscrolled plane model more closely: local damps `detachedPlaneX` toward `target.x + state.scroll`, then renders screen X as `detachedPlaneX - state.scroll`, instead of damping the already-scrolled screen coordinate directly. Detail project switches use `0.056` easing for the first `180ms`; Home-to-Detail keeps `0.05`, and normal geometry remains `0.07`. The refreshed v54 run reports switch-120 reference/local `left/width 810.67/583.34` vs `798.95/585.20`, reducing the old right-side miss, while switch-650 is nearly exact at `328.12/789.06` vs `328.97/788.67` and stable settles exactly. The refreshed v60 Home-to-Detail run stayed stable: the 300ms-ish plane sample is close at `333.3/773.45` vs `336.14/767.73`, with stable exact and no runtime exceptions. Final regression also separated pgn enter opacity from the long transform easing so Home-to-Detail 120ms digits are painted while still entering from split sides. Verification: `node --check src/main.js`, refreshed `scripts/capture-detail-switch-compare-v54.mjs`, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-detail-plane-offset-v27.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-v15.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-fx-v19.mjs`, and `npm run build`.
  Latest v81 evidence: Work media switching now preserves the reference site's sparse media slot model instead of compressing Work media into consecutive local photo indices. Live `_A.config.data.work[0].media` reports `[0,2,3,4,6,8,9]`, and local fallback now uses that reference offset pattern with a seven-slot window, keeping explicit `workMedia` overrides available for future manifest control. Before this patch, v53 Work switch drove local `workIndex 0 -> 1`, leaving the active frame `60px` too high at stable and about `31px` high at 120ms. After the patch, refreshed v53 reports reference/local `workIndex 2` on both sides: switch-120 frame top `222.95` vs `222.07` (`-0.88px`), active layer top `689.76` vs `694.72` (`+4.96px`), switch-300 frame top `270.01` vs `270.70` (`+0.69px`), and stable frame top `271.5` vs `271.5`. The local `/nian-nian-002` Work media window now verifies slots `1,3,4,5,7,9,10`, while non-media slots such as `0,2,6,8` remain in the global rail but are hidden/non-interactive. Verification: `node --check src/main.js`, `node --check scripts/qa-work-media-window-v32.mjs`, `node --check scripts/qa-work-media-order-direction-v34.mjs`, `node --check scripts/qa-work-rail-v18.mjs`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-layer-noninvolved-v50.mjs`, `node --check scripts/qa-work-loaded-return-v22.mjs`, refreshed `scripts/capture-work-switch-compare-v53.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-rail-v18.mjs`, `scripts/qa-work-media-order-direction-v34.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-keyboard-v12.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, `scripts/qa-work-thumb-reentry-v47.mjs`, and `npm run build`.
  Latest v82 evidence: Work lazy-loading QA now follows the same seven-slot sparse media model instead of the older five-contiguous-thumbnail assumption. `scripts/qa-work-media-lazy-v48.mjs` now checks the source for `WORK_ENTRY_REVEAL_DELAY`, `WORK_SWITCH_REVEAL_DELAY`, and the `mediaOrder * 80` thumbnail queue, then verifies `/nian-nian-002` loads no Work large media before entering Work, loads only active large media index `1` on entry, shows only Work thumbnails `1,3,4,5,7,9,10`, switches ArrowDown to sparse index `3`, keeps only visited large media `1,3` loaded during the same Work session, and resets active large opacity on fresh Work re-entry. Verification: `node --check scripts/qa-work-media-lazy-v48.mjs`, `scripts/qa-work-media-lazy-v48.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `npm run build`.
  Latest v83 evidence: the local project color system now mirrors the live reference config instead of generating saturated palettes from each photo. A CDP query against `window._A.config.data.work` captured the reference 30-item `color.bg` / `color.txt["255"]` / `color.work` sequence, and `src/main.js` now maps local photo indices through `REFERENCE_PROJECT_COLORS`. The 001 -> 002 Detail switch also snaps `state.textRgb` to the incoming project text token so the 120ms switch frame uses reference-like black text instead of slowly damping from the previous gold. The refreshed `/tmp/compare-v54-detail-switch-120ms.jpg` shows the local gray/black project state much closer to the reference gray/black frame; telemetry reports reference/local active plane left `825.05px` vs `826.89px` and width `577.2px` vs `573.38px`, while `scripts/qa-home-base-palette-v43.mjs` now verifies Home remains base black/gold, Detail 001 -> 002 at 120ms has `--text-rgb: 30, 30, 30`, Detail settles to `--surface-rgb: 190, 190, 190`, and Work uses reference `--work-rgb` within one RGB channel of `168, 168, 168`. Verification: `node --check src/main.js`, `node --check scripts/qa-home-base-palette-v43.mjs`, `scripts/qa-home-base-palette-v43.mjs`, `scripts/qa-webgl-background-v64.mjs`, `scripts/qa-work-color-v31.mjs`, `scripts/qa-work-active-frame-color-v35.mjs`, refreshed `scripts/capture-detail-switch-compare-v54.mjs`, and `npm run build`.
  Latest v84 evidence: Work internal large-image opacity now follows the live reference CSS rule instead of the previous slow local default. A CDP stylesheet read from `https://aristidebenoist.com/static/css/d.css?2` reports `.w-l-img.fx { opacity: 1; transition: opacity 1000ms cubic-bezier(0.39, 0.575, 0.565, 1); }` and `.w-l-bg.fx { opacity: 0; transition: opacity 100ms linear 1000ms; }`. Local `.work-layer-img` now uses the same `1000ms` default while preserving the separate `650ms` Work-entry override. `scripts/qa-work-layer-fx-v19.mjs` now verifies 120ms and 300ms Work internal switches remain on the blank surface with active image opacity `0`, then stable index `2` reaches computed image opacity `1`; `scripts/qa-work-loaded-return-v22.mjs` verifies a previously loaded Work image returns immediately at opacity `1` while first-time loads still wait through the blank-surface gate. Verification: `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, refreshed `scripts/capture-work-switch-compare-v53.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, and `npm run build`.
  Latest v85 evidence: the hidden `/studio` upload room is now more usable without changing the photo-processing pipeline. The form supports a `titlePrefix` field, displays selected file count and aggregate size before upload, disables all controls while the request is in flight, exposes a Close action, and still clears `workImageDecodeCache` before rebuilding the WebGL gallery after successful uploads. Verification: `node --check src/main.js` and `npm run build`.
  Latest v86 evidence: Work internal media-switch reveal timing has been recalibrated against a fresh live v53 reference/local capture. The previous local `WORK_SWITCH_REVEAL_DELAY = 600` over-held the incoming large image, producing reference/local 300ms active image opacity `0.306057` vs `0`. A direct `100ms` source gate over-corrected because local assets decode faster than the live reference path, producing `0.240908` vs `0.641142`. The current `210ms` local decode-compensated gate preserves the reference blank 120ms frame (`0` vs `0`) and aligns 300ms opacity at reference `0.435288` vs local `0.448858`, while keeping active-layer progress close (`0.946` vs `0.9554`) and stable opacity `1` in both. Verification: `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, refreshed `scripts/capture-work-switch-compare-v53.mjs`, `scripts/qa-work-media-lazy-v48.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, and `npm run build`.
  Latest v87 evidence: repeated fresh v53 Work-switch captures showed the `210ms` local decode compensation was still too eager when the local WebP decoded faster than the live reference media. Before this patch, repeat runs reported reference/local 300ms active image opacity `0.240802` vs `0.460874` and `0.305902` vs `0.447989`, while 120ms stayed blank. A first `300ms` gate improved the match but still visibly outran a later reference run where the live image remained blank at 300ms. The internal switch gate is now `WORK_SWITCH_REVEAL_DELAY = 340`: the refreshed live comparison preserves the 120ms blank frame, reports 300ms opacity reference/local `0` vs `0.151045`, and keeps active-layer/frame progress within `0.02`. The live reference can still vary sharply with network/decode timing; one 340ms run had reference stable opacity only `0.559829` while local had already reached `1`, so this remains a source-site/runtime variability caveat rather than a fully solved deterministic match. Verification: `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-fx-v19.mjs`, refreshed `SUMMARY_ONLY=1 node scripts/capture-work-switch-compare-v53.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-media-lazy-v48.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-loaded-return-v22.mjs`, and `npm run build`.
  Latest v88 evidence: Detail project-switch early geometry now separates the reference-like scroll lag from the plane geometry lag. The first `180ms` of an in-Detail project switch uses `DETAIL_SWITCH_EARLY_SCROLL_EASE = 0.052` and `DETAIL_SWITCH_EARLY_PLANE_EASE = 0.052`, while Home-to-Detail keeps its existing entry branch. Before this patch, refreshed v54 runs showed local 120ms target/current geometry drifting by tens to more than 100 pixels depending on runtime timing, for example reference/local current plane `left/width 924.67/534.75` vs `805.19/582.56`. After the patch, repeated refreshed v54 runs report 120ms current plane close to reference: `843.68/569.25` vs `838.14/571.62` in one run, and `843.90/569.15` vs `855.02/564.42` in the next; 650ms and stable states remain aligned. The same pass keeps Home-to-Detail stable: refreshed v60 reports 300ms local plane `334.63/770.75` vs reference `339.49/761.14`, and stable exact. Verification: `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, repeated `SUMMARY_ONLY=1 node scripts/capture-detail-switch-compare-v54.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-plane-offset-v27.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-meta-switch-v26.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-title-mode-move-v33.mjs`, `SUMMARY_ONLY=1 node scripts/capture-home-detail-entry-compare-v60.mjs`, and `npm run build`.
  Latest v89 evidence: Work first-entry large-image reveal now follows the live reference CSS/source more closely instead of keeping the old fast entry override. Reference CSS uses `.w-l-img.fx { opacity: 1; transition: opacity 1000ms cubic-bezier(.39,.575,.565,1); }`, and reference `class b.imgLoad()` applies a `100ms` delay for large Work media after decode. Local now removes the `650ms` `.is-work-entry` fade override and uses `WORK_ENTRY_REVEAL_DELAY = 100`, while leaving the separately tuned internal Work switch gate at `WORK_SWITCH_REVEAL_DELAY = 340`. Before this patch, `scripts/qa-work-entry-opacity-v52.mjs` reported local Work-entry 300ms active image opacity `0.234035`, visibly faster than the fresh live v51 reference capture around `0.0744641`. An overcorrection to `220ms` held opacity at `0`, so the final reference-source delay is `100ms`; the refreshed v52 run reports local 300ms opacity `0.136678`, loaded `true`, active layer top `363.29px`, and stable opacity `1`. The side-by-side `/tmp/compare-v51-work-entry-300ms.jpg` now shows the local main image in a pale mid-reveal state instead of reading like a settled image. Verification: `node --check src/main.js`, `node --check scripts/qa-work-entry-opacity-v52.mjs`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-entry-opacity-v52.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-fx-v19.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-media-lazy-v48.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-midtransition-compare-v51.mjs`, and `npm run build`.
  Latest v90 evidence: Home base color now matches the live reference runtime state instead of the previous near-white local UI. A direct CDP read from the reference reports `_A.color.txt.rgb255: [186,196,184]`, and computed Home colors for `#n0`, `#n1-0`, `#n2`, and `#n3` are all `rgb(186, 196, 184)`. Local now sets `BASE_TEXT_RGB = [186, 196, 184]`, derives `--ink`, `--muted`, and `--soft` from `--text-rgb`, and draws the Home pagination canvas from `state.textRgb` rather than a hard-coded `[236,235,227]`. The Home pagination base opacity also now matches the reference alpha model: refreshed v61 stable reports reference/local `alphaPixels/alphaSum` both `2700 / 137700`, and the 160ms wheel frame is close at reference `2724 / 138057` vs local `2730 / 138690`. Home drag still tracks the reference path after the color/alpha patch: refreshed v76 reports 200ms target index `0.533` on both sides, current index `0.248` reference vs `0.283` local, and settled `0.533` vs `0.532`. Verification: `node --check src/main.js`, `node --check scripts/qa-home-base-palette-v43.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-base-palette-v43.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-wheel-compare-v61.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-drag-compare-v76.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-wheel-v21.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-pagination-wave-v29.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-webgl-background-v64.mjs`, and `npm run build`.
  Latest v91 evidence: Detail in-project switching now nudges the first 180ms of the scroll/plane handoff closer to the live reference after repeated v54 captures showed local could still lag rightward at 120ms. Local now uses `DETAIL_SWITCH_EARLY_SCROLL_EASE = 0.056` and `DETAIL_SWITCH_EARLY_PLANE_EASE = 0.06`. Before this pass, a refreshed 120ms sample reported reference/local current plane `left/width 825.05/577.2` vs `871.77/557.29` (and one timing-sensitive run was worse at `954.98/521.8`). After the patch, repeated refreshed v54 runs report 120ms `825.04/577.2` vs `830.47/575.78`, then `843.69/569.26` vs `822.14/579.33`; 650ms and stable states remain effectively exact. Home-to-Detail still holds: refreshed v60 reports 300ms plane `334.74/770.52` local vs `337.83/765.12` reference, 750ms `324.89/790.21` vs `325.04/789.94`, and stable exact. Verification: `node --check src/main.js`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, `node --check scripts/qa-detail-plane-offset-v27.mjs`, `node --check scripts/qa-detail-meta-switch-v26.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-detail-switch-compare-v54.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-detail-entry-compare-v60.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-plane-offset-v27.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-meta-switch-v26.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-title-mode-move-v33.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-plane-target-snap-v30.mjs`, and `npm run build`.
  Latest v92 evidence: the local top `.pgn` DOM now mirrors the reference site's persistent 30-slot structure instead of only mounting/painting one real-photo item. A direct CDP sample of the live reference reports `count: 30` and `rootOpacityNonZero: 30` in both Home and Detail; before this patch local reported `count: 17`, Home `rootOpacityNonZero: 0`, and Detail `rootOpacityNonZero: 1`. Local now renders 30 visual pgn slots, maps the 17 real photos onto those slots, keeps all roots opacity-visible, and collapses non-painted roots to `0 x 0` so hidden digits do not draw. Refreshed v71 Detail-drag comparison now reports reference/local `pgnVisible: 30` for before, 120ms, 260ms, and settled, while plane geometry remains close: at drag260 reference/local current plane `left/width 515.53/408.95` vs `518.98/402.04`, and settled `681.16/77.68` vs `681.11/77.79`. Refreshed v60 also shows local Home pgn now present at `top 50 / 1440 x 14` instead of missing; remaining pgn drift is that the reference Home sample still has small residual digit translation (`-16.95/-18.83`) while local is already settled. Verification: `node --check src/main.js`, `node --check scripts/capture-detail-drag-compare-v71.mjs`, `node --check scripts/qa-pgn-v15.mjs`, `node --check scripts/qa-pgn-home-entry-v44.mjs`, `node --check scripts/qa-pagination-canvas-v16.mjs`, `node --check scripts/qa-detail-drag-carry-v38.mjs`, `node --check scripts/qa-pgn-out-split-v39.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-detail-drag-compare-v71.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-v15.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-home-entry-v44.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pagination-canvas-v16.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-drag-carry-v38.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-plane-target-snap-v30.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-wheel-v21.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-detail-entry-compare-v60.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-out-split-v39.mjs`, and `npm run build`.
  Impact: the site can look close in still frames while still feeling off during dragging, wheel latency, project switching, and work media switching.
  Fix: continue porting exact reference values and transition state machines, then compare mid-transition frames.

- [P2] Extra non-reference overlay layer has been removed
  Location: `index.html`, `src/main.ts`, `src/styles.css`.
  Evidence: reference live capture has two canvases: `gl` and `c2d`. Local verification now has two canvases: `webgl` and `.pagination-canvas`.
  Impact: removing the invented overlay makes the rendering structure closer to the reference and prevents non-reference grain/veil artifacts.
  Fix: keep future image effects inside the main plane shader or pagination canvas, matching the reference architecture.

- [P2] Extra non-reference bottom progress rule has been removed
  Location: `src/main.ts`, `src/styles.css`.
  Evidence: matched home wheel screenshots showed a bright local bottom progress line that is not part of the reference home surface. The DOM, style rules, and update loop for `bottom-progress` have been deleted.
  Impact: removes a visible artifact that made the local page read as a custom gallery rather than the reference interaction surface.
  Fix: keep progress/position feedback in the pagination canvas, as the reference does.

- [P2] Extra non-reference global cursor layer has been removed
  Location: `src/main.ts`, `src/styles.css`.
  Evidence: reference CSS/source does not contain a global `.scene-cursor` layer or `cursor: none`; the reference cursor data is used for hit testing and polygon morphs inside project/explore controls. Local verification now reports `sceneCursorCount: 0`, `cursorStyle: auto`, and two canvases.
  Impact: removes a screenshot-visible crosshair artifact that was not part of the reference surface.
  Fix: keep cursor behavior scoped to reference controls and compare hover-state crops before marking this area complete.

- [P2] SVG control hover behavior is now live-verified
  Location: `src/main.ts`, `src/styles.css`.
  Evidence: v57 live hover capture shows reference Project stays on the cross points during symbol hover, while reference Explore morphs from the cross points to the downward arrow on root hover. Local now mirrors that split behavior: Project keeps `9.207,10.508...` unchanged, Explore ends at `7,11.042 6.499,10.542...`. The side-by-side crops are `/tmp/compare-v57-control-project-symbol-hover-crop.jpg` and `/tmp/compare-v57-control-explore-root-hover-crop.jpg`; the v57 script, `qa-reference-hover-v20`, and `qa-project-control-v24` all report no failures.
  Impact: removes a subtle but visible non-reference hover affordance from the top Project control while preserving the source site's Explore polygon morph.
  Fix: keep `scripts/capture-control-hover-compare-v57.mjs` in the regression set for future control edits.

**Open Questions**
- None blocking from the user. The task remains open because full 1:1 reproduction is not yet proven.

**Implementation Checklist**
- Port remaining reference project-switch sequencing from classes `O`, `I`, `P`, `b`, and title/letter motion classes.
- Continue live reference/local mid-transition comparisons for home scroll latency, work entry, work media switching, and the remaining Detail typography/content-structure drift.
- Verify desktop and mobile states separately before declaring completion.
- Keep `/studio` upload flow intact while changing visual effects.

**Follow-up Polish**
- Keep live hover crops in the control regression set when changing Project or Explore controls.
- Exact typography metrics and per-character title placement for every generated title.
- Continue live-frame comparison of Work media loading choreography against the reference after the new lazy `imgLoad`-style lifecycle.
- Exact work-mode media slot mapping and top/bottom media layer choreography.

**Source Visual Truth**
- Live reference captures from this pass:
  - `/tmp/ref-v51-detail-stable.png`
  - `/tmp/ref-v51-work-entry-300ms.png`
  - `/tmp/ref-v51-work-stable.png`
  - `/tmp/ref-live-home-rest-1440.jpg`
  - `/tmp/ref-live-home-hover-1440.jpg`
  - `/tmp/ref-live-home-wheel-mid-1440.jpg`
  - `/tmp/ref-live-detail-switch-mid-1440.jpg`
  - `/tmp/ref-live-work-switch-mid-1440.jpg`
  - `/tmp/ref-v53-work-switch-before.png`
  - `/tmp/ref-v53-work-switch-120ms.png`
  - `/tmp/ref-v53-work-switch-300ms.png`
  - `/tmp/ref-v53-work-switch-stable.png`
  - `/tmp/ref-v54-detail-switch-before.png`
  - `/tmp/ref-v54-detail-switch-120ms.png`
  - `/tmp/ref-v54-detail-switch-650ms.png`
  - `/tmp/ref-v54-detail-switch-stable.png`
  - `/tmp/ref-v57-control-project-symbol-hover.png`
  - `/tmp/ref-v57-control-explore-root-hover.png`
  - `/tmp/ref-v60-home-detail-before.png`
  - `/tmp/ref-v60-home-detail-120ms.png`
  - `/tmp/ref-v60-home-detail-300ms.png`
  - `/tmp/ref-v60-home-detail-750ms.png`
  - `/tmp/ref-v60-home-detail-stable.png`
  - `/tmp/ref-v61-home-wheel-before.png`
  - `/tmp/ref-v61-home-wheel-80ms.png`
  - `/tmp/ref-v61-home-wheel-160ms.png`
  - `/tmp/ref-v61-home-wheel-320ms.png`
  - `/tmp/ref-v61-home-wheel-stable.png`
  - `/tmp/ref-v66-mobile-home.png`
  - `/tmp/ref-v66-mobile-after-enter.png`
  - `/tmp/ref-v71-detail-drag-before.png`
  - `/tmp/ref-v71-detail-drag-120ms.png`
  - `/tmp/ref-v71-detail-drag-260ms.png`
  - `/tmp/ref-v71-detail-drag-settled.png`
- Reference source:
  - `/tmp/aristide-current-d.js`
  - `/tmp/aristide-current-d.css`

**Implementation Screenshots**
- `/tmp/local-v51-detail-stable.png`
- `/tmp/local-v51-work-entry-300ms.png`
- `/tmp/local-v51-work-stable.png`
- `/tmp/local-v53-work-switch-before.png`
- `/tmp/local-v53-work-switch-120ms.png`
- `/tmp/local-v53-work-switch-300ms.png`
- `/tmp/local-v53-work-switch-stable.png`
- `/tmp/local-v54-detail-switch-before.png`
- `/tmp/local-v54-detail-switch-120ms.png`
- `/tmp/local-v54-detail-switch-650ms.png`
- `/tmp/local-v54-detail-switch-stable.png`
- `/tmp/local-v57-control-project-symbol-hover.png`
- `/tmp/local-v57-control-explore-root-hover.png`
- `/tmp/local-v60-home-detail-before.png`
- `/tmp/local-v60-home-detail-120ms.png`
- `/tmp/local-v60-home-detail-300ms.png`
- `/tmp/local-v60-home-detail-750ms.png`
- `/tmp/local-v60-home-detail-stable.png`
- `/tmp/local-v61-home-wheel-before.png`
- `/tmp/local-v61-home-wheel-80ms.png`
- `/tmp/local-v61-home-wheel-160ms.png`
- `/tmp/local-v61-home-wheel-320ms.png`
- `/tmp/local-v61-home-wheel-stable.png`
- `/tmp/local-v66-mobile-home.png`
- `/tmp/local-v66-mobile-detail.png`
- `/tmp/local-v66-mobile-studio.png`
- `/tmp/local-v71-detail-drag-before.png`
- `/tmp/local-v71-detail-drag-120ms.png`
- `/tmp/local-v71-detail-drag-260ms.png`
- `/tmp/local-v71-detail-drag-settled.png`
- `/tmp/compare-v71-detail-drag-before.jpg`
- `/tmp/compare-v71-detail-drag-120ms.jpg`
- `/tmp/compare-v71-detail-drag-260ms.jpg`
- `/tmp/compare-v71-detail-drag-settled.jpg`
- `/tmp/local-webgl-background-v64-detail.png`
- `/tmp/local-webgl-background-v64-work.png`
- `/tmp/local-shader-light-v28-detail.png`
- `/tmp/local-shader-light-v28-work-entering.png`
- `/tmp/local-shader-light-v28-work-stable.png`
- `/tmp/compare-v60-home-detail-before.jpg`
- `/tmp/compare-v60-home-detail-120ms.jpg`
- `/tmp/compare-v60-home-detail-300ms.jpg`
- `/tmp/compare-v60-home-detail-750ms.jpg`
- `/tmp/compare-v60-home-detail-stable.jpg`
- `/tmp/compare-v61-home-wheel-before.jpg`
- `/tmp/compare-v61-home-wheel-80ms.jpg`
- `/tmp/compare-v61-home-wheel-160ms.jpg`
- `/tmp/compare-v61-home-wheel-320ms.jpg`
- `/tmp/compare-v61-home-wheel-stable.jpg`
- `/tmp/compare-v66-mobile-home.jpg`
- `/tmp/compare-v66-mobile-detail.jpg`
- `/tmp/compare-v54-detail-switch-before.jpg`
- `/tmp/compare-v54-detail-switch-120ms.jpg`
- `/tmp/compare-v54-detail-switch-650ms.jpg`
- `/tmp/compare-v54-detail-switch-stable.jpg`
- `/tmp/compare-v57-control-project-symbol-hover-crop.jpg`
- `/tmp/compare-v57-control-explore-root-hover-crop.jpg`
- `/tmp/compare-v53-work-switch-before.jpg`
- `/tmp/compare-v53-work-switch-120ms.jpg`
- `/tmp/compare-v53-work-switch-300ms.jpg`
- `/tmp/compare-v53-work-switch-stable.jpg`
- `/tmp/compare-v51-detail-stable.jpg`
- `/tmp/compare-v51-work-entry-300ms.jpg`
- `/tmp/compare-v51-work-stable.jpg`
- `/tmp/local-no-overlay-work-002-1440.jpg`
- `/tmp/local-final-palette-cream-work-002-1440.jpg`
- `/tmp/local-strong-palette-detail-switch-mid-1440.jpg`
- `/tmp/local-matched-rest-no-progress-1440.jpg`
- `/tmp/local-matched-wheel-mid-no-progress-1440.jpg`
- `/tmp/local-matched-wheel-after-no-progress-1440.jpg`
- `/tmp/matched-rest-ref-local-no-progress.jpg`
- `/tmp/matched-wheel-mid-ref-local-no-progress.jpg`
- `/tmp/local-detail-switch-300ms-single-title-1440.jpg`
- `/tmp/local-detail-switch-900ms-single-title-1440.jpg`
- `/tmp/detail-switch-ref-local-single-title-compare.jpg`
- `/tmp/local-work-before-rail-spacing-1440.jpg`
- `/tmp/local-work-switch-300ms-rail-spacing-1440.jpg`
- `/tmp/local-pagination-real-count-home-1440.jpg`
- `/tmp/local-pagination-detail-stable-canvas-hidden-1440.jpg`
- `/tmp/pagination-home-ref-local-real-count-compare.jpg`
- `/tmp/pagination-detail-ref-local-hidden-canvas-compare.jpg`
- `/tmp/local-home-no-scene-cursor-hover-1440.jpg`
- `/tmp/local-home-no-scene-cursor-wheel-1440.jpg`
- `/tmp/local-work-morph-stable-1440.jpg`
- `/tmp/local-work-morph-hover-1440.jpg`
- `/tmp/local-work-visit-morph-hover-1440.jpg`
- `/tmp/local-work-morph-switch-300ms-1440.jpg`
- `/tmp/work-morph-stable-ref-local-compare.jpg`
- `/tmp/work-morph-switch-300ms-ref-local-compare.jpg`
- `/tmp/local-title-compact-detail-1440.jpg`
- `/tmp/local-title-compact-work-1440.jpg`
- `/tmp/local-title-compact-work-switch-300ms-1440.jpg`
- `/tmp/title-compact-work-ref-local-compare.jpg`
- `/tmp/title-compact-work-switch-ref-local-compare.jpg`
- `/tmp/local-uvscale-home-1440.jpg`
- `/tmp/local-uvscale-detail-1440.jpg`
- `/tmp/local-uvscale-work-1440.jpg`
- `/tmp/local-uvscale-work-switch-300ms-1440.jpg`
- `/tmp/uvscale-home-ref-local-compare.jpg`
- `/tmp/uvscale-work-ref-local-compare.jpg`
- `/tmp/uvscale-work-switch-ref-local-compare.jpg`
- `/tmp/local-refstate-alpha-home-1440.jpg`
- `/tmp/local-refstate-alpha-detail-1440.jpg`
- `/tmp/local-refstate-alpha-work-1440.jpg`
- `/tmp/local-refstate-alpha-work-switch-300ms-1440.jpg`
- `/tmp/refstate-alpha-home-ref-local-compare.jpg`
- `/tmp/refstate-alpha-detail-ref-local-compare.jpg`
- `/tmp/refstate-alpha-work-ref-local-compare.jpg`
- `/tmp/refstate-alpha-work-switch-ref-local-compare.jpg`
- `/tmp/local-ref-work-dommedia-stable-1440.jpg`
- `/tmp/local-ref-work-dommedia-switch-300ms-1440.jpg`
- `/tmp/local-ref-dommedia-home-1440.jpg`
- `/tmp/local-ref-dommedia-detail-1440.jpg`
- `/tmp/ref-work-dommedia-stable-compare.jpg`
- `/tmp/ref-work-dommedia-switch-compare.jpg`
- `/tmp/local-ref-work-fx-enter-150ms-v2-1440.jpg`
- `/tmp/local-ref-work-fx-stable-v2-1440.jpg`
- `/tmp/local-ref-work-fx-switch-300ms-v2-1440.jpg`
- `/tmp/ref-work-fx-stable-v2-compare.jpg`
- `/tmp/ref-work-fx-switch-v2-compare.jpg`
- `/tmp/local-work-fx-bg-thumb-150ms-v3-1440.png`
- `/tmp/local-title-clip-detail-entry-270ms-v4-1440.png`
- `/tmp/local-title-home-work-move-300ms-v4b-1440.png`
- `/tmp/local-project-control-work-v5-1440.png`
- `/tmp/local-pgn-detail-v6-1440.png`
- `/tmp/local-project-control-work-v6-1440.png`
- `/tmp/local-home-pagination-right-v6-1440.png`
- `/tmp/local-detail-drag-out-mid-v7-1440.png`
- `/tmp/local-detail-drag-out-final-v7-1440.png`
- `/tmp/local-explore-detail-v8-1440.png`
- `/tmp/local-explore-detail-hover-v8-1440.png`
- `/tmp/local-explore-work-v8-1440.png`
- `/tmp/local-detail-side-click-before-v9-1440.png`
- `/tmp/local-detail-side-click-after-v9-1440.png`
- `/tmp/local-work-no-visit-detail-v10-1440.png`
- `/tmp/local-work-no-visit-work-v10-1440.png`
- `/tmp/local-work-entry-lt3d-v11-150ms.png`
- `/tmp/local-work-entry-lt3d-v11-450ms.png`
- `/tmp/local-work-entry-lt3d-v11-stable.png`
- `/tmp/local-work-keyboard-v12-arrowright.png`
- `/tmp/local-work-keyboard-v12-about.png`
- `/tmp/local-home-detail-keyboard-v13-hover-enter.png`
- `/tmp/local-home-detail-keyboard-v13-about.png`
- `/tmp/local-home-detail-keyboard-v13-work-return.png`
- `/tmp/local-loader-texture-v14-loading.png`
- `/tmp/local-loader-texture-v14-final.png`
- `/tmp/local-pgn-v15-before.png`
- `/tmp/local-pgn-v15-switch-120ms.png`
- `/tmp/local-pgn-v15-switch-650ms.png`
- `/tmp/local-pgn-v15-work.png`
- `/tmp/local-pagination-canvas-v16-home.png`
- `/tmp/local-pagination-canvas-v16-detail.png`
- `/tmp/local-pagination-canvas-v16-detail-hover.png`
- `/tmp/local-pagination-canvas-v16-work.png`
- `/tmp/local-work-wheel-v17-down.png`
- `/tmp/local-work-wheel-v17-up.png`
- `/tmp/local-work-wheel-v17-arrowup-close.png`
- `/tmp/local-work-rail-v18-index2.png`
- `/tmp/local-work-layer-fx-v19-120ms.png`
- `/tmp/local-work-layer-fx-v19-300ms.png`
- `/tmp/local-work-layer-fx-v19-stable.png`
- `/tmp/local-reference-hover-v20-home-enter.png`
- `/tmp/local-reference-hover-v20-detail-side-click.png`
- `/tmp/local-home-wheel-v21-mid.png`
- `/tmp/local-home-wheel-v21-after.png`
- `/tmp/local-detail-wheel-v21-out.png`
- `/tmp/local-work-loaded-return-v22-first-switch.png`
- `/tmp/local-work-loaded-return-v22-return.png`
- `/tmp/local-work-loaded-return-v22-stable.png`
- `/tmp/local-title-work-delay-v23-detail.png`
- `/tmp/local-title-work-delay-v23-300ms.png`
- `/tmp/local-title-work-delay-v23-stable.png`
- `/tmp/local-project-control-v24-450ms.png`
- `/tmp/local-project-control-v24-stable.png`
- `/tmp/local-pagination-scale-v25-home.png`
- `/tmp/local-pagination-scale-v25-detail.png`
- `/tmp/local-pagination-scale-v25-work.png`
- `/tmp/local-detail-meta-switch-v26-before.png`
- `/tmp/local-detail-meta-switch-v26-120ms.png`
- `/tmp/local-detail-meta-switch-v26-650ms.png`
- `/tmp/local-detail-meta-switch-v26-stable.png`
- `/tmp/local-detail-plane-offset-v27-before.png`
- `/tmp/local-detail-plane-offset-v27-120ms.png`
- `/tmp/local-detail-plane-offset-v27-650ms.png`
- `/tmp/local-detail-plane-offset-v27-stable.png`
- `/tmp/local-shader-light-v28-detail.png`
- `/tmp/local-shader-light-v28-work-entering.png`
- `/tmp/local-shader-light-v28-work-stable.png`
- `/tmp/local-home-pagination-wave-v29-before.png`
- `/tmp/local-home-pagination-wave-v29-mid.png`
- `/tmp/local-home-pagination-wave-v29-after.png`
- `/tmp/local-plane-target-snap-v30-home.png`
- `/tmp/local-plane-target-snap-v30-detail-80ms.png`
- `/tmp/local-plane-target-snap-v30-detail-stable.png`
- `/tmp/local-plane-target-snap-v30-work-80ms.png`
- `/tmp/local-plane-target-snap-v30-detail-return-80ms.png`
- `/tmp/local-work-color-v31-stable.png`
- `/tmp/local-work-color-v31-switch.png`
- `/tmp/local-work-media-window-v32-stable.png`
- `/tmp/local-work-media-window-v32-next.png`
- `/tmp/local-work-media-window-v32-close.png`
- `/tmp/local-title-mode-move-v33-detail.png`
- `/tmp/local-title-mode-move-v33-work-300ms.png`
- `/tmp/local-title-mode-move-v33-work-stable.png`
- `/tmp/local-title-mode-move-v33-detail-return-300ms.png`
- `/tmp/local-work-media-order-direction-v34-before.png`
- `/tmp/local-work-media-order-direction-v34-180ms.png`
- `/tmp/local-work-media-order-direction-v34-stable.png`
- `/tmp/local-work-active-frame-color-v35-stable.png`
- `/tmp/local-work-active-frame-color-v35-switch.png`
- `/tmp/local-work-rail-breakpoint-v36-1000.png`
- `/tmp/local-work-rail-breakpoint-v36-1440.png`
- `/tmp/local-work-plane-photo-y-v37-detail.png`
- `/tmp/local-work-plane-photo-y-v37-work-360ms.png`
- `/tmp/local-work-plane-photo-y-v37-work-stable.png`
- `/tmp/local-detail-drag-carry-v38-before.png`
- `/tmp/local-detail-drag-carry-v38-crossed.png`
- `/tmp/local-detail-drag-carry-v38-carried.png`
- `/tmp/local-detail-drag-carry-v38-final.png`
- `/tmp/local-pgn-out-split-v39-before.png`
- `/tmp/local-pgn-out-split-v39-80ms.png`
- `/tmp/local-pgn-out-split-v39-settled.png`
- `/tmp/local-reference-dpr-v40-home.png`
- `/tmp/local-reference-dpr-v40-detail.png`
- `/tmp/local-about-plane-source-v41-detail.png`
- `/tmp/local-about-plane-source-v41-detail-about.png`
- `/tmp/local-about-plane-source-v41-work.png`
- `/tmp/local-about-plane-source-v41-work-about.png`
- `/tmp/local-home-target-lag-v42-after-key.png`
- `/tmp/local-home-target-lag-v42-immediate-enter.png`
- `/tmp/local-home-base-palette-v43-home.png`
- `/tmp/local-home-base-palette-v43-home-scrolled.png`
- `/tmp/local-home-base-palette-v43-detail.png`
- `/tmp/local-home-base-palette-v43-work.png`
- `/tmp/local-pgn-home-entry-v44-home.png`
- `/tmp/local-pgn-home-entry-v44-120ms.png`
- `/tmp/local-pgn-home-entry-v44-stable.png`
- `/tmp/local-home-drag-edge-v45-before.png`
- `/tmp/local-home-drag-edge-v45-outside-min.png`
- `/tmp/local-home-drag-edge-v45-reversed.png`
- `/tmp/local-work-reentry-fx-v46-first-stable.png`
- `/tmp/local-work-reentry-fx-v46-60ms.png`
- `/tmp/local-work-reentry-fx-v46-240ms.png`
- `/tmp/local-work-reentry-fx-v46-stable.png`
- `/tmp/local-work-thumb-reentry-v47-first-stable.png`
- `/tmp/local-work-thumb-reentry-v47-40ms.png`
- `/tmp/local-work-thumb-reentry-v47-130ms.png`
- `/tmp/local-work-thumb-reentry-v47-stable.png`
- `/tmp/local-work-media-lazy-v48-detail.png`
- `/tmp/local-work-media-lazy-v48-work-stable.png`
- `/tmp/local-work-media-lazy-v48-switch-80ms.png`
- `/tmp/local-work-media-lazy-v48-switch-stable.png`
- `/tmp/local-work-media-lazy-v48-reentry-60ms.png`
- `/tmp/local-loader-nav-v49-loading.png`
- `/tmp/local-loader-nav-v49-index.png`
- `/tmp/local-work-layer-noninvolved-v50-before.png`
- `/tmp/local-work-layer-noninvolved-v50-180ms.png`
- `/tmp/local-work-layer-noninvolved-v50-stable.png`

**Viewport**
- 1440 x 900 desktop.
- 390 x 844 mobile, DPR 2, iPhone user agent.

**State**
- Local detail route: `/nian-nian-002`.
- Local work state opened with `e`.
- Local mobile Home/Detail fallback and mobile `/studio`.
- Live reference captured home, hover, wheel, detail switch, and work switch states.
- Live reference mobile captured Home and post-Enter/click state.

**Full-View Comparison Evidence**
- Current pass used live reference screenshots and same-input local browser screenshots listed above. Stitched comparisons were created for matched rest and matched home wheel-mid states.
- Mobile fallback pass used same-size reference/local mobile captures and stitched comparisons for Home and Detail-like post-click states.

**Focused Region Comparison Evidence**
- Focused evidence from this pass:
  - Mobile fallback verification: `/tmp/ref-v66-mobile-home.png`, `/tmp/ref-v66-mobile-after-enter.png`, `/tmp/local-v66-mobile-home.png`, `/tmp/local-v66-mobile-detail.png`, and `/tmp/local-v66-mobile-studio.png` verify the responsive branch. The live reference at 390 x 844 reports no canvas and the desktop-only message. Local Home and Detail now report fallback visible with zero visible canvases, while `/studio` stays visible and usable so the upload backdoor is not blocked on mobile.
  - Canvas count verification: local now has exactly `webgl` and `.pagination-canvas`.
  - Palette verification: local computed `--surface-rgb: 140, 195, 166`, `--text-rgb: 252, 241, 204`, and `color: rgb(252, 241, 204)`.
  - Same-input home wheel verification: reference and local were captured at 1440 x 900 after the same wait and `scrollY: -900` browser input; local had no console errors and no bottom progress artifact after the patch.
  - Detail switch verification: `/tmp/detail-switch-ref-local-single-title-compare.jpg` compares the reference detail mid-switch to the local 300ms switch frame after title density and timing patches.
  - Detail meta/copy switch verification: `/tmp/local-detail-meta-switch-v26-before.png`, `/tmp/local-detail-meta-switch-v26-120ms.png`, `/tmp/local-detail-meta-switch-v26-650ms.png`, and `/tmp/local-detail-meta-switch-v26-stable.png` verify the new previous-info layer for Detail project switches. Starting on `/nian-nian-002`, the previous meta/copy layers were hidden while the active frame read `002 / 017`. At 120ms after `ArrowRight`, the shell had `is-project-switching`, active frame read `003 / 017`, previous frame stayed `002 / 017`, previous meta displayed as `grid`, previous copy displayed as `block`, previous meta/copy ran `text-line-drop-out` for `0.5s`, and the active meta/copy kept reference-like entry delays of `0.4s` and `0.6s`. At 650ms, the previous layer retained the completed out animation during the switch window; stable state removed `is-project-switching`, hid the previous layers, and kept active frame `003 / 017` with no runtime exceptions.
  - Detail plane offset verification: `/tmp/local-detail-plane-offset-v27-before.png`, `/tmp/local-detail-plane-offset-v27-120ms.png`, `/tmp/local-detail-plane-offset-v27-650ms.png`, and `/tmp/local-detail-plane-offset-v27-stable.png` verify the reference-like `x.targ - x.curr` offset for WebGL planes during Detail switches. Starting on `/nian-nian-002`, active plane target X and offset were `0`. At 120ms after `ArrowRight`, active frame read `003 / 017`, `is-project-switching` was present, `detailScrollOffsetPx` measured `46.62`, active plane X/target X measured `0.9023 / 0.1036`, and outgoing plane X/target X measured `-0.7147 / -1.5179`. At 650ms the offset had damped to `2.56`; stable state removed `is-project-switching`, settled offset to `0`, kept active frame `003 / 017`, and reported no runtime exceptions.
  - Plane target-state verification: `/tmp/local-plane-target-snap-v30-home.png`, `/tmp/local-plane-target-snap-v30-detail-80ms.png`, `/tmp/local-plane-target-snap-v30-detail-stable.png`, `/tmp/local-plane-target-snap-v30-work-80ms.png`, and `/tmp/local-plane-target-snap-v30-detail-return-80ms.png` verify the WebGL plane target state now follows the reference `pCurr -> pTarg` model more closely. Static QA confirmed plane geometry uses immediate `planeDetailTarget` and `planeWorkTarget` values instead of damped `detailMix`/`workMix` as the layout target. Runtime CDP at 1440 x 900 reported Home `detailTarget/workTarget: 0 / 0`; 80ms after entering Detail, `detailTarget` had already snapped to `1` while `detailMix` was still `0.3993`; stable Detail reached `detailMix: 0.995`; 80ms after entering Work, `workTarget` had already snapped to `1` while `workMix` was still `0.3039`; 80ms after returning to Detail, `workTarget` snapped back to `0` while `workMix` was still decaying at `0.3709`. This removes the local target interpolation layer and lets the current plane state chase the new reference target like the source renderer.
  - About plane-source verification: `/tmp/local-about-plane-source-v41-detail.png`, `/tmp/local-about-plane-source-v41-detail-about.png`, `/tmp/local-about-plane-source-v41-work.png`, and `/tmp/local-about-plane-source-v41-work-about.png` verify the reference `mode === "a"` behavior for WebGL plane targets. Static QA confirmed About plane layout now derives from `getPlaneSourceMode()`, Detail-sourced About passes `isAbout` into `referenceModeInRect()`, Detail side planes receive the extra reference `in.gapX`, and the canvas exposes source-mode, active texture-scale target, and side-plane target telemetry. Runtime CDP at 1440 x 900 on `/nian-nian-002` reported stable Detail `planeSourceMode: detail`, right-side next-plane left `1252.05px`; 180ms after pressing `a`, About reported `planeSourceMode: detail`, `activePlaneDetailTarget/workTarget: 1 / 0`, alpha target `0`, active texture-scale target `0.15`, and the next-plane target shifted to `1388.85px`, exactly one `in.gapX` outward. Stable Work reported `planeSourceMode: work`, active `photoYTarget: -0.1`; 180ms after pressing `a`, Work-sourced About kept `activePlaneDetailTarget/workTarget: 1 / 1`, alpha target `0`, texture-scale target `0`, `photoYTarget: -0.1`, and no runtime exceptions. Regression after this pass kept `qa-home-detail-keyboard-v13`, `qa-plane-target-snap-v30`, `qa-work-plane-photo-y-v37`, and `qa-shader-light-v28` green.
  - Home target-lag verification: `/tmp/local-home-target-lag-v42-after-key.png` and `/tmp/local-home-target-lag-v42-immediate-enter.png` verify the reference Home keyboard path where ArrowRight/Space/ArrowLeft change `x.targ` but do not synchronously change the current project index. Static QA confirmed Home ArrowRight/Space and ArrowLeft call `setHomeScrollTargetIndex()` instead of `setActive()`, and Home pagination clicks now take the same target-only path. Runtime CDP at 1440 x 900 reported ArrowRight from Home `001` kept `current: 001`, `mode: index`, `path: /`, `scrollPx: 0`, and set `targetScrollPx: 630`. A fresh immediate ArrowRight + Enter sequence then opened `/nian-nian-001` rather than `/nian-nian-008`, proving Enter still uses the current/hovered index like reference `indexOver || _A.index`. Regression after this pass kept `qa-home-detail-keyboard-v13`, `qa-home-wheel-v21`, `qa-detail-drag-carry-v38`, and `qa-home-pagination-wave-v29` green.
  - Home wheel visual-pagination verification: `/tmp/compare-v61-home-wheel-before.jpg`, `/tmp/compare-v61-home-wheel-160ms.jpg`, and `/tmp/compare-v61-home-wheel-stable.jpg` compare the live reference and local Home rail in the same image input. The refreshed local rail uses the reference centered c2d coordinate model rather than the previous clipped right-edge placement: live reference reports `workL: 30`, `dpr: 2`, `gapX: 16`, `outWidth: 1`, and `leftOut: 1200`; local reports `count: 30` and `photoCount: 17`. The current v70 pass keeps the same 2700 alpha-pixel Home rail footprint but lowers the Home tick alpha sum to `102600`, matching the live reference top-band luminance after the base-surface correction rather than preserving the older white-surface `137700` alpha-sum proxy. `scripts/qa-home-pagination-wave-v29.mjs` reports Home rest `alphaPixels: 2700`, mid-scroll `2760`, and no runtime exceptions. `scripts/qa-pagination-canvas-v16.mjs` still verifies stable Detail canvas fade-out plus Detail hover revival (`0 -> 52808` alpha sum). `scripts/qa-home-wheel-v21`, `scripts/qa-home-detail-keyboard-v13`, and `scripts/qa-webgl-background-v64` all pass after the brightness calibration.
  - Home base-palette verification: `/tmp/local-home-base-palette-v43-home.png`, `/tmp/local-home-base-palette-v43-home-scrolled.png`, `/tmp/local-home-base-palette-v43-detail.png`, and `/tmp/local-home-base-palette-v43-work.png` verify the reference color-state split from controller `v.resize()`: `out` and `a` use the base color, while `in` and `w` use the active project's colors. Static QA confirmed `BASE_SURFACE_RGB`, `BASE_TEXT_RGB`, `BASE_WORK_RGB`, and mode-gated `usesProjectPalette` targets in `updateUi()`. Runtime CDP at 1440 x 900 now reports Home rest `001` and Home scrolled `008` both keep `paletteMode: base`, `--surface-rgb: 20, 20, 20`, `--text-rgb: 191, 150, 65`, and `--work-rgb: 214, 216, 210`, matching the live reference's sampled black Home surface instead of the previous local white surface. Detail `/nian-nian-002` reported `paletteMode: detail`, `--surface-rgb: 137, 191, 163`, and `--text-rgb: 252, 240, 203`; Work reported `paletteMode: work`, `--surface-rgb: 140, 195, 166`, `--text-rgb: 252, 241, 204`, `--work-rgb: 149, 171, 164`, and no runtime exceptions. Regression after this pass kept `qa-home-wheel-v21`, `qa-home-detail-keyboard-v13`, and `qa-webgl-background-v64` green.
  - WebGL background-plane verification: `/tmp/local-webgl-background-v64-detail.png` and `/tmp/local-webgl-background-v64-work.png` verify the reference `planeBg` architecture instead of relying on CSS background or clear-color alpha. Static QA confirmed `backgroundPlane`, `uHasTexture` values for solid/textured branches, the shared fragment branch `if (uHasTexture != 1)`, and transparent renderer clear. Runtime CDP at 1440 x 900 reported exactly two canvases in both Detail and Work, `backgroundPlane: "1"`, and WebGL background telemetry matching the shell `--surface-rgb`: Detail `145, 197, 170`, Work `140, 195, 166`. `qa-shader-light-v28`, `qa-plane-target-snap-v30`, and `qa-home-base-palette-v43` still pass after this patch.
  - Home-to-Detail pgn entry verification: `/tmp/local-pgn-home-entry-v44-home.png`, `/tmp/local-pgn-home-entry-v44-120ms.png`, and `/tmp/local-pgn-home-entry-v44-stable.png` verify the reference `P.show({ out: true })` split-direction digit entry. Static QA confirmed a new `paginationDigitShowOut` branch, `startPaginationDigitMotion(state.activeIndex, state.activeIndex, { out: true })` on Home/Loading-to-Detail, and separate `aShowOutStart: 101` / `bShowOutStart: -101` starts. Runtime CDP at 1440 x 900 reported Home had no visible pgn DOM digits; 120ms after Enter, Detail `/nian-nian-001` had pgn index `0` with `01` at `translate3d(73.68%, 0, 0)` and `17` at `translate3d(-73.68%, 0, 0)`; stable Detail settled both to `0%`. Regression after this pass kept `qa-pgn-v15`, `qa-home-target-lag-v42`, `qa-home-detail-keyboard-v13`, and `qa-pgn-out-split-v39` green.
  - Home drag edge-reset verification: `/tmp/local-home-drag-edge-v45-before.png`, `/tmp/local-home-drag-edge-v45-outside-min.png`, and `/tmp/local-home-drag-edge-v45-reversed.png` verify the reference `v.move()` boundary behavior where the drag origin is reset while dragging outward at `min` or `max`, avoiding a dead zone when reversing direction. Static QA confirmed `adjustDragOriginAtBounds(clientX, previousClientX)`, the min/max direction checks, the reference formula `clientX - (dragOriginScroll - bound) / sensitivity`, and `dragBoundReset` telemetry. Runtime CDP at 1440 x 900 reported Home rest `targetScrollPx: 0`; after dragging right from x `720` to `1200`, target stayed clamped at `0` with `dragBoundReset: 0`; after reversing left to x `1040`, target immediately moved to `192px`, current advanced to `002`, and no runtime exceptions were reported. Regression after this pass kept `qa-detail-drag-carry-v38`, `qa-home-wheel-v21`, and `qa-home-pagination-wave-v29` green.
  - Work active-plane texture-offset verification: `/tmp/local-work-plane-photo-y-v37-detail.png`, `/tmp/local-work-plane-photo-y-v37-work-360ms.png`, and `/tmp/local-work-plane-photo-y-v37-work-stable.png` verify the active WebGL plane now matches the reference `modeIn()`/`modeW()` split for `pY`. Static QA confirmed Detail `detailPhotoY` is `0`, Work active `workPhotoY` targets `-0.1`, and the canvas exposes active plane `photoY`, `photoYTarget`, `targetH`, and target top telemetry. Runtime CDP at 1440 x 900 on `/nian-nian-002` reported stable Detail `photoY/photoYTarget: 0 / 0`; 360ms after opening Work, `photoY/photoYTarget: -0.0907 / -0.1`, `workMix: 0.8052`, target height `0.8027`, and target top `-470.64px`; stable Work reported `photoY/photoYTarget: -0.1 / -0.1`, `workMix: 0.9998`, target top `-470.64px`, and no runtime exceptions. Regression after this pass also kept `qa-plane-target-snap-v30`, `qa-shader-light-v28`, and `qa-work-layer-fx-v19` green.
  - Work project-color verification: `/tmp/local-work-color-v31-stable.png` and `/tmp/local-work-color-v31-switch.png` verify the Work media color branch now mirrors reference `class b.color()` more closely. Static QA confirmed each generated photo palette includes `work: photoWorkColorFor(rgb, surface)`, the shell exposes `--work-rgb`, and `.work-stage-bg`, `.work-layer-bg`, and `.detail-thumb` consume that variable. Runtime CDP on `/nian-nian-002` reported stable Work `--work-rgb: 148, 171, 163`, distinct from `--surface-rgb: 140, 195, 166` and `--photo-rgb: 151, 162, 162`; the active layer background, active thumb background, and stage background all computed to `rgb(148, 171, 163)`. At 180ms after `ArrowRight`, the active image was still mid-fade with computed opacity `0.0871308`, while the layer and thumb backgrounds remained `rgb(148, 171, 163)` and no runtime exceptions were reported. The v35 active-frame color pass adds `/tmp/local-work-active-frame-color-v35-stable.png` and `/tmp/local-work-active-frame-color-v35-switch.png`: stable and switching Work both report `--text-rgb: 252, 241, 204`, `--work-rgb: 148, 171, 163`, active-frame border `rgb(252, 241, 204)`, frame opacity `0.7`, and active thumb background `rgb(148, 171, 163)`, matching reference `b.color()` where Work media backgrounds use `color.work` while `#w-a` border uses `color.txt.rgb`.
  - Work media-window verification: `/tmp/local-work-media-window-v32-stable.png`, `/tmp/local-work-media-window-v32-next.png`, and `/tmp/local-work-media-window-v32-close.png` verify that Work rail/media selection now follows the reference `class b` idea of a current project `media` list instead of exposing every gallery photo as a Work thumbnail. Static QA confirmed `WORK_MEDIA_WINDOW`, `getWorkMediaIndices()`, `getWorkMediaOrder()`, keyboard traversal through the current media list, `.is-work-media` thumb marking, and the non-media pointer guard. Runtime CDP on `/nian-nian-002` reported stable Work with exactly five visible Work media thumbnails, ordered `002, 003, 004, 005, 006` as work orders `0..4`; the active thumb was `002` with `activeThumbOrder: 0`; hidden non-media thumbnails had opacity `0`, `pointerEvents: none`, `tabIndex: -1`, and `aria-hidden: true`. `ArrowRight` advanced to work order `1` (`003`) while the route stayed `/nian-nian-002`; `ArrowUp` returned to the first media; a second `ArrowUp` closed back to Detail, matching the reference Work keyboard close branch. The v34 media-order direction pass adds `/tmp/local-work-media-order-direction-v34-before.png`, `/tmp/local-work-media-order-direction-v34-180ms.png`, and `/tmp/local-work-media-order-direction-v34-stable.png`: opening `/nian-nian-017` in Work produced fallback media order `017, 016, 015, 014, 013`; pressing `ArrowRight` activated global index `15` while exiting global index `16`, kept `is-switch-forward` even though the global index decreased, moved the new active layer from below at `330.48px`, moved the exiting layer upward at `-499.99px`, and settled the new layer at `0.28px` with no runtime exceptions. Regression runs after this direction patch passed `qa-work-layer-fx-v19`, `qa-work-media-window-v32`, and `qa-work-keyboard-v12`.
  - Dynamic title-density verification: `/tmp/compare-v54-detail-switch-before.jpg`, `/tmp/compare-v54-detail-switch-120ms.jpg`, and `/tmp/compare-v54-detail-switch-stable.jpg` now compare the live reference against the local dynamic title bank. The before-state local title uses `NIAN NIAN / DIARY` rather than the older fixed compact title, and the switch/stable state uses a short `JOY` title so the visual rhythm changes with the project like the reference. Browser metrics from `/tmp/capture-v54-detail-switch-report.json` show before-state local first-line glyph count `8` versus reference `7`, while the switched state matches the reference's 3-glyph title count and closely matches the three x positions. The v59 refresh changes the 120ms outgoing-title behavior from a full overlay into reference-like clipped vertical strips by using an `o3`-style fast-start out curve, preserving opacity, and moving the outgoing glyph spans horizontally through their clipping wrappers. The local follow-up regressions `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, and `scripts/qa-detail-plane-offset-v27.mjs` all reported no failures.
  - Detail info-density verification: the latest `/tmp/capture-v54-detail-switch-report.json` shows the local `.project-shadow-meta` block now matches the reference `.i-l` geometry at `top 805`, `left 310`, `width 410`, and `height 48` across before, switch, and stable states. The right copy block now measures `227.66px` wide against the reference stable/switch width `228.28px`, while preserving the reference `top 832` and `height 20`. The follow-up `scripts/qa-detail-meta-switch-v26.mjs` pass kept the previous active/previous layer choreography intact with no runtime exceptions or failures.
  - Control geometry verification: the latest `/tmp/capture-v54-detail-switch-report.json` shows the top Project control now measures local `top 49 / left 689.5 / width 61 / height 79`, matching the reference `top 49 / left 689.44 / width 61.13 / height 79`. The bottom Explore control now measures local `top 747 / left 693.65 / width 52.70 / height 104`, close to the reference `top 747 / left 693.88 / width 52.25 / height 104`. `scripts/qa-project-control-v24.mjs` and `scripts/qa-reference-hover-v20.mjs` both report no failures after the geometry and letter-spacing patch.
  - Control hover-state verification: `/tmp/compare-v57-control-project-symbol-hover-crop.jpg` and `/tmp/compare-v57-control-explore-root-hover-crop.jpg` compare live reference and local hover crops. Runtime metrics show Project hover is expected not to morph in both reference and local, while Explore root hover is expected to morph in both. `scripts/capture-control-hover-compare-v57.mjs` reports `failures: []`, with no runtime exceptions.
  - Work switch verification: `/tmp/compare-v53-work-switch-120ms.jpg`, `/tmp/compare-v53-work-switch-300ms.jpg`, and `/tmp/compare-v53-work-switch-stable.jpg` were refreshed after the v58 active-frame lerp patch. The latest `/tmp/capture-v53-work-switch-report.json` reports normalized active-frame progress at 120ms as reference `0.6477` vs local `0.6717` (`+0.02`) and at 300ms as reference `0.9533` vs local `0.9562` (`0.00`). Active large-layer progress remains close at 120ms `+0.02` and 300ms `+0.01`, while stable Work aligns at active large top `206.35px` local vs `206.37px` reference and active-frame top `271.5px` in both. This replaces the previous local active-frame early lag introduced by damping `railActiveLerp` back to `.08` during the switch.
  - Work switch source-timing verification: `/tmp/compare-v53-work-switch-120ms.jpg`, `/tmp/compare-v53-work-switch-300ms.jpg`, and `/tmp/compare-v53-work-switch-stable.jpg` were refreshed again after checking live reference opacity against the decode-gated local media path. The current local switch reveal keeps the reference-style decode gate but adds a `240ms` minimum Work-switch reveal gate so local assets do not fade in far earlier than the live site. The fresh live comparison reports switch-120 active opacity `0` for both reference and local; at 300ms reference active opacity is `0.124068` and local is `0.125048`, normalized active-layer progress is reference `0.8463` vs local `0.8405`, active-frame progress is reference `0.9561` vs local `0.9532`, and stable Work matches at active opacity `1` and active-frame top `271.5px` in both.
  - Pagination verification: `/tmp/local-pagination-canvas-v16-home.png`, `/tmp/local-pagination-canvas-v16-detail.png`, `/tmp/local-pagination-canvas-v16-detail-hover.png`, and `/tmp/local-pagination-canvas-v16-work.png` verify the canvas pagination state-machine after porting more of reference class `I`. The latest CDP sampling at 1440 x 900 with fixed reference DPR reported Home `alphaPixels: 840`, `alphaSum: 42840`, no pgn DOM digits, and `canvasOpacity: 1`; stable Detail reported `alphaPixels: 0`, `alphaSum: 0`, one visible pgn at index `1` with text `02 / 17`, and `canvasOpacity: 1`; Detail hover revived the opened rail with `alphaPixels: 80`, `alphaSum: 16256`; Work kept one visible pgn at index `1` with text `02 / 17`, while the canvas rail measured `alphaPixels: 80`, `alphaSum: 16304`. This replaces the older CSS-hidden Detail canvas behavior with reference-like drawable canvas opacity damping.
  - Home pagination wave verification: `/tmp/local-home-pagination-wave-v29-before.png`, `/tmp/local-home-pagination-wave-v29-mid.png`, and `/tmp/local-home-pagination-wave-v29-after.png` verify the Home scroll middle-frame curve and pagination wave against reference formulas from class `I.loop()`. Static QA confirmed the local canvas now uses reference amplitude `30 * latency.x`, tick height `pH.curr + wave`, and wave center `x * workL` rounded to two decimals. Runtime CDP at 1440 x 900 reported rest Home `paginationHeight: 14`, `paginationLatencyLift: 0`, `paginationWaveCenter: 0`, alpha span `15px`, active plane curve `0`, and rotateY `0`. At 160ms after a 900px wheel, active plane curve rose to `0.0386`, rotateY to `-0.0247`, pagination lift to `1.922`, wave center to `0.45`, and alpha span to `17px`. After settling, lift decayed to `0.042`, curve to `0.0039`, rotateY to `-0.003`, alpha span returned to `15px`, frame advanced to `002 / 017`, and no runtime exceptions were reported.
  - Reference DPR verification: `/tmp/local-reference-dpr-v40-home.png` and `/tmp/local-reference-dpr-v40-detail.png` verify renderer backing-store density against the reference source. Static QA confirmed fixed `WEBGL_DPR = 1.5`, fixed `PAGINATION_DPR = 2`, `renderer.setPixelRatio(WEBGL_DPR)`, pagination `const dpr = PAGINATION_DPR`, and 20 x 2 plane vertices through `new THREE.PlaneGeometry(1, 1, 19, 1)`. Runtime CDP at 1440 x 900 reported Home and Detail WebGL backing `2160 x 1350` with ratio `1.5`, pagination backing `2880 x 1800` with ratio `2`, and no runtime exceptions. Regression after this pass kept `qa-pagination-canvas-v16`, `qa-home-pagination-wave-v29`, `qa-plane-target-snap-v30`, and `qa-shader-light-v28` green.
  - Pagination scale verification: `/tmp/local-pagination-scale-v25-home.png`, `/tmp/local-pagination-scale-v25-detail.png`, and `/tmp/local-pagination-scale-v25-work.png` verify reference `class I.propTopH()` sizing after the latest metric patch. Home reports canvas metric height and target height `14`, top target `49`; Detail and Work report base metric `14`, half-height target/current `7`, and top target/current `0`. Visible pgn DOM digits measure `14px` high with `12px` font size and `14px` line height in Detail and Work, with no runtime exceptions.
  - Cursor verification: local browser state reports no `.scene-cursor`, default cursor style, two canvases, and no console errors after removing the non-reference overlay.
  - Polygon morph verification: `/tmp/compare-v57-control-project-symbol-hover-crop.jpg` and `/tmp/compare-v57-control-explore-root-hover-crop.jpg` confirm the reference split: top Project remains a cross on hover, while bottom Explore morphs from cross to arrow. The v24 Work project-control pass adds `/tmp/local-project-control-v24-450ms.png` and `/tmp/local-project-control-v24-stable.png`: after pressing `e`, the 450ms Work frame reports symbol timing `1.4s / 0.6s`, line timing `1.2s / 0.6s`, label timing `1.4s / 0.4s`, with transforms at positive Y (`15.4`, `17.6`, `11.9538`) so all three enter from below like reference `L` show motion. Stable Work reports each transform at `0`, root opacity `1`, visibility `visible`, and no runtime exceptions.
  - Title verification: `/tmp/local-title-compact-detail-1440.jpg` confirms detail title glyphs no longer use the old unbounded fallback spread, while `/tmp/title-compact-work-ref-local-compare.jpg` compares the restored two-line Work title against the live reference Work state. The latest title-clipping pass adds `/tmp/local-title-clip-detail-entry-270ms-v4-1440.png` and `/tmp/local-title-home-work-move-300ms-v4b-1440.png`: browser metrics show title line overflow is now `hidden`, per-character wrapper overflow is `hidden`, inner spans run `title-char-in` for `1.6s`, Detail x positions for the first four letters start at `[27.28, 163.68, 300.08, 436.48]`, and the Work 300ms frame has moved them to `[4.04, 94.07, 148.29, 238.35]` on a `1.2s` transition with no console errors. The v23 title delay pass adds `/tmp/local-title-work-delay-v23-detail.png`, `/tmp/local-title-work-delay-v23-300ms.png`, and `/tmp/local-title-work-delay-v23-stable.png`: Detail keeps reference-like return stagger delays `[0s, 0.01s, 0.02s, 0.03s]`; after pressing `e`, the Work 300ms frame reports all first-line character transition delays as `0s`, line scale moving at `0.863597`, and first-four x positions `[11.97, 90.26, 136.79, 212.45]`; stable Work reports scale `0.84`, x positions `[8.02, 76.03, 106.61, 171.53]`, and no runtime exceptions. This aligns the Work entry branch with reference `class C.move()` where `end === "work"` uses `c = 0` and `p = 1` for every glyph instead of a local per-letter drag. The v33 title mode-move pass adds `/tmp/local-title-mode-move-v33-detail.png`, `/tmp/local-title-mode-move-v33-work-300ms.png`, `/tmp/local-title-mode-move-v33-work-stable.png`, and `/tmp/local-title-mode-move-v33-detail-return-300ms.png`: Work 300ms reports `is-title-mode-moving`, no `is-project-switching`, title span animation names `none`, visible zero-translation inner spans, first-four x positions around `[11.76, 89.48, 135.14, 210.21]`, stable Work reports `is-title-mode-settled`, and Work-to-Detail return keeps the same no-reveal-replay behavior with no runtime exceptions.
  - Shader UV verification: `/tmp/uvscale-home-ref-local-compare.jpg`, `/tmp/uvscale-work-ref-local-compare.jpg`, and `/tmp/uvscale-work-switch-ref-local-compare.jpg` compare the local reference-style centered `uUvScale` crop against live reference states. Browser verification reported two canvases and no console errors after the shader change.
  - Shader light/alpha verification: `/tmp/local-shader-light-v28-detail.png`, `/tmp/local-shader-light-v28-work-entering.png`, and `/tmp/local-shader-light-v28-work-stable.png` verify the WebGL plane shader now follows the reference fragment formula more closely. Static QA confirmed the shader uses unclamped `float light = vCurve + uColorMix;` instead of the previous local `clamp(...)`, and active-plane opacity damping now matches the reference hide/show split of `0.23` and `0.07`. Runtime CDP at 1440 x 900 reported stable Detail on `/nian-nian-002` with active plane alpha/target `0.989 / 1`, light `0.9986`, multiply `0`, visible pgn `02 17`, and no runtime exceptions. Work entry at 360ms reported alpha/target `0.9988 / 1`, light `0.9999`; stable Work reported alpha `1`, target `1`, light `1`, and no runtime exceptions.
  - Plane state verification: `/tmp/refstate-alpha-detail-ref-local-compare.jpg`, `/tmp/refstate-alpha-work-ref-local-compare.jpg`, and `/tmp/refstate-alpha-work-switch-ref-local-compare.jpg` compare the reference `modeIn`/`modeW` opacity and media-switch states against the local pass after porting all-plane alpha, active-photo multiply, and detail side hover light targets. Browser verification reported modes `index`, `detail`, `work`, and `work` mid-switch, two canvases, and no console errors.
  - Work DOM media verification: `/tmp/ref-work-dommedia-stable-compare.jpg` and `/tmp/ref-work-dommedia-switch-compare.jpg` compare the reference Work state against the local pass after removing local DOM image filters/scale and restoring the source `w-s` rail rhythm. Browser metrics at 1440 x 900 reported thumb `80 x 45`, rail gap `15px`, active frame y `151.55`, active large-media transform `matrix(1, 0, 0, 1, 0, 0)`, layer filter `none`, thumb filter `none`, and no console errors.
  - Work image fx verification: `/tmp/local-ref-work-fx-enter-150ms-v2-1440.jpg`, `/tmp/local-ref-work-fx-stable-v2-1440.jpg`, `/tmp/local-ref-work-fx-switch-300ms-v2-1440.jpg`, and `/tmp/local-work-fx-bg-thumb-150ms-v3-1440.png` verify delayed Work media reveal. The latest Work entry run now separates CSS target opacity from computed visual opacity like the reference `.w-l-img.fx` path: at 150ms into Work entry, the active large image has target opacity `1.0000` but computed opacity `0`; by 450ms computed opacity reaches `0.641227`; stable Work computes to `1`; the Work large-media background layer measures `1.0000` early and `0.0000` after the delayed `.w-l-bg.fx`-style fade; and the browser reported no runtime exceptions.
  - Work re-entry fx reset verification: `/tmp/local-work-reentry-fx-v46-first-stable.png`, `/tmp/local-work-reentry-fx-v46-60ms.png`, `/tmp/local-work-reentry-fx-v46-240ms.png`, and `/tmp/local-work-reentry-fx-v46-stable.png` verify the reference `class b.init()` boundary where a fresh Work visit resets image fx, while same-session media return remains loaded. Static QA confirmed `primeWorkLayerMotion({ preserveImages: preserveWorkFx })`, the `is-work-media-resetting` no-transition CSS reset, and the reflow gate before reveal. Runtime CDP on `/nian-nian-002` reported first Work stable index `1` with computed image opacity `1`; after pressing `p` back to Detail and `e` into Work again, the 60ms frame had active Y `547.49px`, style opacity `0`, computed opacity `0`, background opacity `1`, and no visible stale image; at 240ms, style opacity was `1` while computed opacity was mid-fade at `0.358013`; stable re-entry settled at Y `0.12px`, computed opacity `1`, background opacity `0`, and no runtime exceptions. Regression after this pass kept `qa-work-loaded-return-v22`, `qa-work-layer-fx-v19`, `qa-work-media-window-v32`, `qa-work-media-order-direction-v34`, and `qa-work-entry-v11` green.
  - Work thumbnail re-entry queue verification: `/tmp/local-work-thumb-reentry-v47-first-stable.png`, `/tmp/local-work-thumb-reentry-v47-40ms.png`, `/tmp/local-work-thumb-reentry-v47-130ms.png`, and `/tmp/local-work-thumb-reentry-v47-stable.png` verify the reference `imgLoad(i, "s")` queue where Work thumbnails reset and reveal by media order. Static QA confirmed the `mediaOrder * 80` image reveal gate, the reset/no-transition class path, and the `.is-thumb-image-visible` transition branch. Runtime CDP on `/nian-nian-002` reported first Work stable with five visible thumbnails, all computed image opacity `1`. After returning to Detail and re-entering Work, the 40ms frame reported order `0` mid-fade at computed image opacity `0.0371415`, order `1` still gated with `visibleClass: false` and computed opacity `0`, and lower orders still hidden. At 130ms, order `0` advanced to `0.319087`, order `1` had begun its delayed fade at `0.189334`, order `2` was just entering at `0.0620568`, and later orders stayed hidden. Stable re-entry settled all five thumbnails at computed opacity `1` and near-zero Y, with no runtime exceptions.
  - Work media lazy lifecycle verification: `/tmp/local-work-media-lazy-v48-detail.png`, `/tmp/local-work-media-lazy-v48-work-stable.png`, `/tmp/local-work-media-lazy-v48-switch-80ms.png`, `/tmp/local-work-media-lazy-v48-switch-stable.png`, and `/tmp/local-work-media-lazy-v48-reentry-60ms.png` verify the reference `class b.init()` / `imgLoad()` resource lifecycle rather than only opacity timing. Static QA confirmed `EMPTY_MEDIA_SRC`, `data-work-src`, `data-thumb-src`, `resetWorkLayerImage()`, `requestWorkLayerImageLoad()`, `resetWorkThumbImage()`, `requestWorkThumbImageLoad()`, the 100ms large-media reveal delay, the `mediaOrder * 80` thumbnail delay, and stale decode guards via dataset load tokens. Runtime CDP on `/nian-nian-002` reported Detail-before-Work with all 17 Work large images still at `src="data:,"`, no loaded Work layer indices, and no non-Work thumbnail backgrounds. First stable Work loaded only active large image index `1`, kept 16 Work layers empty, loaded exactly the five current Work thumbnails, and kept non-Work thumbnail backgrounds empty. After `ArrowDown`, the 80ms Work-switch frame had active large image index `2` loaded but computed opacity `0`, while same-session stable Work retained only visited large indices `[1, 2]`. Fresh Detail-to-Work re-entry reset back to only active index `1` loaded with computed opacity `0` at 60ms, while thumbnail order `0` was just fading (`0.0746135`) and later orders were still gated. Verification after this patch passed `npm run build`, `qa-work-media-lazy-v48`, `qa-work-reentry-fx-v46`, `qa-work-thumb-reentry-v47`, `qa-work-loaded-return-v22`, `qa-work-media-window-v32`, `qa-work-layer-fx-v19`, and `qa-work-entry-v11`.
  - Work non-involved layer verification: `/tmp/local-work-layer-noninvolved-v50-before.png`, `/tmp/local-work-layer-noninvolved-v50-180ms.png`, and `/tmp/local-work-layer-noninvolved-v50-stable.png` verify the reference `class b.up(i)` behavior where only the outgoing and incoming large-media layers are reassigned during a Work media switch. Static QA confirmed the local switch code now keeps the reference `exitY` / `enterY` branches for `previousIndex` and `nextIndex` and no longer contains the former `const side = index < nextIndex` mass-reposition branch. Runtime CDP at 1440 x 900 on `/nian-nian-002` reported stable Work index `1` with non-involved layer `0` below the stage at `830.55px`; 180ms after `ArrowRight`, previous layer `1` was exiting upward at `-497.32px`, new active layer `2` was entering from below at `333.14px`, and non-involved layer `0` stayed below at `830.55px` without active/exiting classes or resource load. Stable Work index `2` settled active Y to `0.33px`, left layer `0` below at `830.55px`, and reported no runtime exceptions. Verification after this patch passed `npm run build`, `qa-work-layer-noninvolved-v50`, `qa-work-media-order-direction-v34`, `qa-work-layer-fx-v19`, `qa-work-media-lazy-v48`, `qa-work-reentry-fx-v46`, and `qa-work-thumb-reentry-v47`.
  - Loader/nav intro verification: `/tmp/local-loader-nav-v49-loading.png` and `/tmp/local-loader-nav-v49-index.png` verify the reference `#load` / `class W` intro direction. Static QA confirmed loader digits start at `translate3d(-110%, 0, 0)`, exit through `digit-exit 500ms cubic-bezier(0.55, 0.055, 0.675, 0.19)`, finish at `translate3d(110%, 0, 0)`, and the loader container no longer fades or translates upward. Runtime CDP at 1440 x 900 reported early loading mode with loader opacity `1`, transform `none`, digit transform `matrix(1, 0, 0, 1, 0, 0)`, and no Y movement. After release to Home, the loader still had opacity `1` and transform `none`, the digit animation was `digit-exit` with duration `0.5s`, the first digit translated horizontally to X `18.0469` with Y `0`, and top nav wordmark/about characters used `split-char-in` over `1.6s` with `0.2s` initial delay. Regression after this pass kept `qa-loader-v14`, `qa-home-target-lag-v42`, `qa-pgn-home-entry-v44`, and `qa-home-detail-keyboard-v13` green.
  - Work `lT3d` entry verification: `/tmp/local-work-entry-lt3d-v11-150ms.png`, `/tmp/local-work-entry-lt3d-v11-450ms.png`, and `/tmp/local-work-entry-lt3d-v11-stable.png` verify the active Work large-media layer now starts from the reference `class b` lower `lTarg` position instead of appearing centered immediately. Chrome CDP metrics at 1440 x 900 reported Detail pre-entry work-layer Y `830.55px`; after clicking `EXPLORE`, the 150ms Work frame measured active Y `376.20px`, image opacity `0.2095`, background opacity `1.0000`, static stage transform `matrix(1, 0, 0, 1, -433.328, -243.75)`, and active transform `matrix(1, 0, 0, 1, 0, 376.2)`; the 450ms frame measured active Y `42.82px`, image opacity `0.8629`, and background opacity `1.0000`; stable Work measured active Y `0.03px`, image opacity `0.9997`, and background opacity `0.0000`. This moves the entry behavior closer to reference `b.resize()` where active `lT3d.curr` starts at `lTarg` and damps to `0`, with `.w-l-bg.fx` fading later and no extra stage translate animation.
  - Work keyboard controller verification: `/tmp/local-work-keyboard-v12-arrowright.png` and `/tmp/local-work-keyboard-v12-about.png` verify the reference-like `class b.key()` keyboard paths. Starting from `/nian-nian-001` Work, CDP metrics reported `activeWorkIndex: 0`; after `ArrowRight`, mode stayed `work`, `activeWorkIndex: 1`, `exitingWorkIndex: 0`, active layer Y `184.42px`, CSS target opacity `1.0000`, computed image opacity `0.202238`, and active thumb index `1`; after `ArrowUp`, mode stayed `work`, `activeWorkIndex: 0`, `exitingWorkIndex: 1`, active layer Y `-138.66px`, and computed image opacity `1`; a second `ArrowUp` at index `0` changed mode to `detail`; re-entering Work and pressing `a` changed mode to `about`, URL `/about`, and `aboutHidden: false`. This mirrors the reference behavior where ArrowUp at the first media closes Work, while `a` triggers the top navigation action.
  - Work wheel controller verification: `/tmp/local-work-wheel-v17-down.png`, `/tmp/local-work-wheel-v17-up.png`, and `/tmp/local-work-wheel-v17-arrowup-close.png` verify the reference-like absence of wheel navigation in `class b`. Starting from `/nian-nian-001` Work, CDP reported `mode: work`, `activeLayerIndex: 0`, `activeThumbIndex: 0`, and `visiblePgn: 1`. After a `deltaY: 900` wheel event, mode stayed `work`, path stayed `/nian-nian-001`, and active layer index stayed `0`; after `deltaY: -900`, mode still stayed `work`. Pressing ArrowUp at the first media then changed mode to `detail`, preserving the reference keyboard close path while removing the previous non-reference wheel-to-Detail shortcut.
  - Work rail controller verification: `/tmp/local-work-rail-v18-index2.png` verifies the reference-like fixed Work rail offset and active frame travel. Starting from `/nian-nian-001` Work, CDP metrics at 1440 x 900 reported `railOffset: 0` across indices 0, 1, and 2; expected step `60`; index 1 `railActiveY: 59.89`; index 2 `railActiveY: 119.88`; active frame right offset `44`; thumbnail right offset `50`; and no runtime exceptions. This matches the reference `#w-a`/`#w-s-w` right-edge geometry and `aYTarg = 80 * h / 1200` step, instead of using the Detail rail's scroll-compensating offset. The v36 breakpoint pass adds `/tmp/local-work-rail-breakpoint-v36-1000.png` and `/tmp/local-work-rail-breakpoint-v36-1440.png`: at 1000 x 900, Work rail computed `display: none` with a zero-size rect, matching reference `@media(max-width:1150px){#w-a,#w-s-w{display:none}}`; at 1440 x 900, Work rail remained `display: block`, opacity `0.74`, active frame opacity `0.7`, and rect `92 x 597`, preserving the desktop Work rail state.
  - Work layer/image fx split verification: `/tmp/local-work-layer-fx-v19-120ms.png`, `/tmp/local-work-layer-fx-v19-300ms.png`, and `/tmp/local-work-layer-fx-v19-stable.png` verify that Work large media now follow the reference wrapper/image split. In the latest run, at the early switch sample after `ArrowRight`, the active Work layer is a `DIV` wrapper at Y `331.97px` with wrapper opacity `1`, CSS image target opacity `1`, computed child `IMG` opacity `0.0371818`, and moving background opacity `1`; the exiting wrapper stays opacity `1` while translating to Y `-497.63px`. At 300ms, active Y is `55.05px`, computed child image opacity is `0.583738`, and background opacity remains `1`; stable index 1 reports Y `0.04px`, computed child image opacity `1`, background opacity `0`, and no runtime exceptions. This prevents the non-reference behavior where the translating wrapper itself faded instead of leaving opacity ownership to `.w-l-img.fx`.
  - Work loaded-return verification: `/tmp/local-work-loaded-return-v22-first-switch.png`, `/tmp/local-work-loaded-return-v22-return.png`, and `/tmp/local-work-loaded-return-v22-stable.png` verify the reference `imgVisible.l` behavior and static Work stage container. At 1440 x 900, stable Work index 0 measured target opacity `1` and computed image opacity `1`; first switch to index 1 measured active Y `331.81px`, target opacity `1`, and computed image opacity `0.0370296`, proving first-time large media still fades in through CSS. After index 1 settled, switching back to already-visible index 0 measured active Y `-444.18px`, target opacity `1`, and computed image opacity `1`, so the image no longer replays the fade. The same run reported Work stage opacity `1`, centered transform `matrix(1, 0, 0, 1, -433.328, -243.75)`, transition duration `0s`, shell `::before` content `none`, and no runtime exceptions.
  - Reference hover-hit verification: `/tmp/local-reference-hover-v20-home-enter.png` and `/tmp/local-reference-hover-v20-detail-side-click.png` verify Home/Detail hover and click now use reference-style current pixel rectangles rather than Three raycasting. At 1440 x 900, moving to `{ x: 810, y: 450 }` on a settled Home surface and pressing Enter opened `/nian-nian-002` with frame `002 / 017`; navigating to `/nian-nian-002` Detail and clicking `{ x: 1270, y: 450 }` switched in place to `/nian-nian-003` with frame `003 / 017`; no runtime exceptions were reported. This follows the reference `v.over()` behavior where Home/Detail hit testing checks current plane rectangles and skips the active plane in Detail.
  - Home/Detail wheel latency verification: `/tmp/local-home-wheel-v21-mid.png`, `/tmp/local-home-wheel-v21-after.png`, and `/tmp/local-detail-wheel-v21-out.png` verify the reference-like `class x`/`v.sXFn()` wheel path and latency accumulator. At 1440 x 900, Home before/mid/after stayed in `mode: index` with two canvases; after a 900px wheel, the frame advanced from `001 / 017` to `002 / 017`. On `/nian-nian-002` Detail, a tiny `deltaY: 1` wheel changed mode to `index`, path `/`, hid the pgn digits, and reported no runtime exceptions. This removes the previous local 42px threshold and aligns Detail wheel exit with the reference `modeOut()` branch.
  - Home/Detail keyboard controller verification: `/tmp/local-home-detail-keyboard-v13-hover-enter.png`, `/tmp/local-home-detail-keyboard-v13-about.png`, and `/tmp/local-home-detail-keyboard-v13-work-return.png` verify the reference-like `class v.key()` paths for Home/Detail plus About return state. Chrome CDP metrics at 1440 x 900 reported Home hover at the second plane followed by `Enter` opened Detail at `/nian-nian-002`, `activeThumbIndex: 1`; pressing `a` in Detail opened About at `/about`, `aboutHidden: false`, and `aboutLabel: CLOSE`; pressing `c` returned to Detail at `/nian-nian-002`; pressing `e` opened Work; after `ArrowRight`, Work advanced to `activeWorkIndex: 2`; pressing `a` opened About; pressing `Escape` returned to Work with `activeWorkIndex: 2`; and the run reported no runtime exceptions. This closes the local mismatch where About always returned to Home and Home Enter ignored the hovered plane.
  - Loader texture-gate verification: `/tmp/local-loader-texture-v14-loading.png` and `/tmp/local-loader-texture-v14-final.png` verify the reference-like loader path from source class `d`, where the counter is driven by loaded texture count rather than a wall-clock fake progress. The CDP run disabled cache, applied a 640 KB/s local network throttle, and observed loader values `[1, 7, 13, 18, 24, 30, 36, 48, 53, 59, 65, 71, 77, 83, 88, 100]`. While shell mode was still `loading`, media resource entries advanced to `15` and loader text stopped at `088`; only after media entries reached `17` did the shell switch to `index` with loader `100`. The final sample was `mode: index`, `mediaEntries: 17`, `loaderValue: 100`, loader opacity near zero, and no runtime exceptions. This replaces the previous fixed 1650ms fake counter with texture-settled release behavior that matches the reference formula `Math.round(99 / texL * no) + 1`.
  - Pagination digit state-machine verification: `/tmp/local-pgn-v15-before.png`, `/tmp/local-pgn-v15-switch-120ms.png`, `/tmp/local-pgn-v15-switch-650ms.png`, and `/tmp/local-pgn-v15-work.png` verify a closer port of reference classes `I` and `P` for `.pgn-a/.pgn-b`. Starting at `/nian-nian-002`, CDP reported one visible pgn at index `1` with text `02 / 17`. After `ArrowRight`, the 120ms frame had two pgn layers: previous index `1` marked `is-leaving`, opacity `0.5567`, with both `pgn-a` and `pgn-b` moving left at `translate3d(-44.78%, 0px, 0px)`, while new index `2` entered from the right with opacity `0.2616` and transform `translate3d(74.58%, 0px, 0px)`. At 650ms, only index `2` remained visible, opacity `0.9837`, transform `translate3d(1.65%, 0px, 0px)`. Stable Detail and Work both reported one visible pgn at index `2` with text `03 / 17`, and no runtime exceptions. This replaces the previous static `.is-visible` toggle with a direction-aware `P.show`/`P.hide`-style 200ms leave and delayed 1200ms enter path, and keeps pgn open in Work like reference `pgnIsOpen()` includes `w`. The v39 out-split pass adds `/tmp/local-pgn-out-split-v39-before.png`, `/tmp/local-pgn-out-split-v39-80ms.png`, and `/tmp/local-pgn-out-split-v39-settled.png`: pressing `ArrowUp` from Detail `/nian-nian-002` exited to Home while previous pgn index `1` stayed in a leaving layer; at 80ms, `pgn-a` moved right to `20.59%` and `pgn-b` moved left to `-20.59%`, both at opacity `0.7962`; after the 200ms-style out animation settled, no pgn layers remained visible. Regression after this pass kept `qa-pgn-v15`, `qa-home-wheel-v21`, `qa-detail-drag-carry-v38`, and `qa-plane-target-snap-v30` green.
  - Project control verification: `/tmp/local-project-control-work-v5-1440.png`, `/tmp/local-project-control-work-v6-1440.png`, and `/tmp/compare-v54-detail-switch-before.jpg` verify the top project control after replacing the old local number/button cluster with the reference-like current number, symbol, total number, line, and `PROJECTS` stack. The latest v54 live comparison at 1440 x 900 reports Detail project control `top 49 / left 689.5 / 61 x 77` against the reference `top 49 / left 689.44 / 61.13 x 79`, with Work animation timing still passing `scripts/qa-project-control-v24.mjs`.
  - Pagination digit verification: `/tmp/local-pgn-detail-v6-1440.png` verifies the reference-like `.pgn-a/.pgn-b` DOM digits in Detail. Chrome CDP metrics at 1440 x 900 reported active pgn index `1`, text `02` at x `1325.72`, text `17` at x `1384.39`, both at y `50`, both opacity `1`, and both transforms `matrix(1, 0, 0, 1, 0, 0)`. Later v15/v16 checks confirm Work now keeps one pgn digit visible too, aligning with the reference `pgnIsOpen()` condition for `w`. The top pagination metrics now use the reference-style right-aligned `window.innerWidth - count * gap / 2` baseline instead of the earlier centered local baseline.
  - Home pagination verification: `/tmp/local-home-pagination-right-v6-1440.png` verifies the right-aligned top canvas pagination after the metrics change. Chrome CDP metrics at 1440 x 900 reported `canvasOpacity: 1`, `visiblePgn: 0`, and ABOUT at x `1348.73`, matching the reference relationship where the thin pagination marks sit at the top-right beside/below ABOUT rather than centered.
  - Detail drag-out verification: `/tmp/local-detail-drag-out-mid-v7-1440.png` and `/tmp/local-detail-drag-out-final-v7-1440.png` verify the reference-like `modeIn -> modeOut` drag gesture. Chrome CDP metrics at 1440 x 900 reported before state `mode: detail`, URL `/nian-nian-002`, and `visiblePgn: 1`; after a horizontal mouse drag, mid state `mode: index`, URL `/`, `visiblePgn: 0`; final state remained `mode: index`, URL `/`, `visiblePgn: 0`. This mirrors the reference home controller behavior where dragging while in `in` mode calls `modeOut()` and subsequent movement continues to drive the horizontal strip. The updated v38 drag-carry pass adds `/tmp/local-detail-drag-carry-v38-before.png`, `/tmp/local-detail-drag-carry-v38-crossed.png`, `/tmp/local-detail-drag-carry-v38-carried.png`, and `/tmp/local-detail-drag-carry-v38-final.png`: starting Detail `/nian-nian-002` reported `scrollPx/targetScrollPx: 90 / 90`; after dragging from x `720` to `560`, mode switched to Home but target stayed at `90px`, matching the reference split where the threshold-crossing `modeOut()` event does not immediately call `xTargUp()`; continuing to x `520` then raised target to `522px`; after the out animation settled, `visiblePgn` returned to `0`; final Home scroll settled near `517px`, frame `007 / 017`, with no runtime exceptions. Regression after this pass kept `qa-detail-drag-carry-v38`, `qa-home-drag-edge-v45`, `qa-home-wheel-v21`, `qa-home-detail-keyboard-v13`, `qa-pagination-canvas-v16`, `qa-pgn-home-entry-v44`, and `qa-home-pagination-wave-v29` green.
  - Explore control verification: `/tmp/local-explore-detail-v8-1440.png`, `/tmp/local-explore-detail-hover-v8-1440.png`, `/tmp/local-explore-work-v8-1440.png`, and `/tmp/compare-v54-detail-switch-before.jpg` verify the Detail bottom Explore geometry, hover morph, and click-through Work state after porting the reference `.e` timing. Earlier v8 CDP metrics verified the morph and timing; the latest v54 live comparison now reports Detail Explore `top 747 / height 104`, matching the reference `top 747 / height 104`, with line height updated to `53px` while keeping the same `1.4s 0.6s`, `1.2s 0.8s`, and `1.4s 0.8s` child animations.
  - Detail side-plane click verification: `/tmp/local-detail-side-click-before-v9-1440.png` and `/tmp/local-detail-side-click-after-v9-1440.png` verify the reference `in`-mode short-click behavior on a neighboring plane. Chrome CDP clicked point `{ x: 1270, y: 450 }` on the right side plane at 1440 x 900; before state was URL `/nian-nian-002`, `mode: detail`, current `002`, frame `002 / 017`, pgn `02 / 17`; after state was URL `/nian-nian-003`, `mode: detail`, current `003`, frame `003 / 017`, pgn `03 / 17`. This matches the reference controller path where `up()` calls `modeIn(pOver)` for a non-active plane while already in `in` mode. No runtime exceptions were reported; console output only contained Chrome WebGL readback performance warnings, favicon 404, and the pre-existing hidden-studio password-form recommendation.
  - Work no-visit verification: `/tmp/local-work-no-visit-detail-v10-1440.png` and `/tmp/local-work-no-visit-work-v10-1440.png` verify the reference-like conditional `#v` behavior. Current photo data has no `visitUrl`, so Detail still exposes `EXPLORE` with opacity `1`, `pointerEvents: auto`, `aria-hidden: false`, and `tabIndex: 0`; after clicking into Work, shell mode is `work`, `has-work-visit: false`, and the same bottom control is hidden with opacity `0`, `pointerEvents: none`, `aria-hidden: true`, and `tabIndex: -1`. This mirrors the reference source where the bottom visit link is shown only when `work[index].visit` exists.

**Required Fidelity Surfaces**
- Fonts and typography: still close but not proven exact. Needs focused cropped comparison.
- Spacing and layout rhythm: broadly close in desktop stills, but mid-motion states still need 1:1 validation.
- Colors and visual tokens: improved this pass with project-level color space, still needs per-state tuning.
- Image quality and asset fidelity: WebP variants load correctly; shader behavior still being ported toward reference.
- Copy and content: intentionally adapted to family archive wording while preserving reference-like structure.

**Patches Made Since Previous QA Pass**
- Added per-photo project palettes with `surface`, `text`, and `photo` RGB values.
- Damped palette transitions through renderer clear color and CSS custom properties.
- Synchronized legacy `--gold` and `--paper-line` variables to the active text palette.
- Removed the non-reference `#webgl-overlay` canvas, renderer, shader, and CSS.
- Ported reference home wheel axis selection/scaling and 300ms running state behavior.
- Changed Home latency normalization so the second accumulator uses the reference-like plain interpolation formula, while non-index curve/rotation latency decays instead of snapping.
- Updated home plane rotation multiplier to match the reference `-0.4 * latency.rotate` transform.
- Changed Detail wheel handling to call Home/index modeOut immediately on any wheel input, matching the reference `v.sXFn()` path instead of using a local 42px threshold.
- Removed the non-reference bottom progress bar from DOM, CSS, and the update loop.
- Extended project-switch class lifetime to 1650ms and aligned title/info/pagination timing closer to reference durations.
- Replaced the fixed detail/work shadow title with a dynamic project-title bank, including long two-line titles such as `NIAN NIAN / DIARY` and short titles such as `JOY`, so project switches now change title density like the reference.
- Added custom `shadowTitle`/`titleLines` hooks for future photo metadata, while keeping the local generated title bank as the default.
- Added 3-, 4-, 5-, 7-, and 8-glyph Detail title position presets plus matching Work letter-spacing presets, so both long and short project titles can migrate between Detail and Work without losing the reference-style character motion.
- Escaped title text and per-glyph output in the title markup so custom title data can be safely rendered.
- Locked Detail info-grid rows to the reference `12px` rhythm so the four-row block resolves to `48px` tall instead of `52px`.
- Changed the Detail right copy block from a fixed `250px` width to reference-like shrink-to-fit text, and adjusted the family archive line to `AN INTIMATE LOCAL PHOTO ARCHIVE OF HER` so its rendered width lands inside the live reference range.
- Added a `79px` minimum height to the Detail/Work top Project control and applied the reference `-0.02em` letter spacing to the Project/Explore controls, aligning the measured control boxes with live reference geometry.
- Changed title fallback positioning for unknown title lengths to compact by measured glyph width instead of spreading letters across the viewport.
- Replaced local shader-side random horizontal pan/crop with a JS-computed `uUvScale` path that mirrors the reference plane shader's `m` uniform behavior.
- Restored work-mode rail spacing to the reference `5vh` thumbnail plus `1.66667vh` gap rhythm.
- Changed home pagination metrics to use the actual photo count and reference left/open-left spacing math.
- Reworked the pagination canvas so Detail/Work no longer rely on CSS `opacity: 0`; the canvas stays drawable while the JS state machine damps rect opacity to zero in stable Detail/Work and revives the opened active rail on hover.
- Removed the non-reference global `.scene-cursor` DOM, CSS, and update loop, restoring the default cursor.
- Added reference SVG polygon cross/arrow point sets for the top project close control and bottom explore/visit control.
- Added 700ms `o5`-style point interpolation plus pointer/mouse/geometry hover handling for those controls.
- Removed Work-mode pseudo-element cross rules that conflicted with the SVG polygon control.
- Refactored the Detail top Project control so Detail and Work now share the reference center stack: current number, morph symbol, total number, 3px line, and `PROJECTS` label. The latest v54 comparison measures local `61 x 77` against reference `61.13 x 79`.
- Refactored the Detail bottom Explore control to match the reference `.e` structure more closely: top `747px`, height `104px`, bottom `49px`, clipped label, clipped `3px` vertical-line wrapper with a `53px` line, `14px` clipped morph symbol, `9px`/`13px` vertical rhythm, and 1400ms/1200ms/1400ms child-entry timing with 600ms/800ms delays.
- Added `scripts/capture-detail-switch-compare-v54.mjs` to capture live reference and local Detail project switches at before/120ms/650ms/stable, generate side-by-side comparisons, and report Project, Explore, title, info, and plane metrics.
- Made the Work bottom visit control conditional on a real `visitUrl`/`visit` field, mirroring the reference `#v` behavior. Detail `EXPLORE` still enters Work, while Work with no external link hides the bottom visit affordance visually and from tab focus.
- Added generated per-photo `multiply` and `inOverLight` values from each photo palette so uploaded photos automatically feed reference-like plane tinting.
- Changed detail/work WebGL plane opacity targets to stay visible for every plane, matching the reference `modeIn`/`modeW` `o = 1` behavior and relying on layout clipping instead of local alpha suppression.
- Changed non-active detail/work plane multiply to use the active photo's `0.75`/`1` value instead of the previous fixed `0.42`.
- Extended hover hit testing into detail mode so side planes can receive the active photo's `inOverLight` value.
- Added reference-like Detail side-plane short-click switching: clicking a non-active side plane now switches the active photo while staying in Detail, mirroring the source controller's `modeIn(pOver)` branch.
- Removed non-reference grayscale/contrast/brightness filters and scale transforms from DOM work media layers.
- Removed non-reference grayscale/hover-scale treatment from right-rail thumbnails so thumbnails behave like the reference `.w-s-img` fade layer.
- Removed the non-reference active-rail pulse animation during work media switches.
- Added reference-style large Work media delayed reveal with a roughly 100ms `revealAt` gate and opacity damping through the animation loop.
- Changed Work entry large-media motion to start the active layer from the reference lower `lTarg` position and damp it to center, instead of placing the active layer at `0` immediately and only fading it in.
- Aligned Work keyboard handling with reference `class b.key()`: Tab is trapped, `a` opens About, ArrowRight/ArrowDown/Space advance, ArrowLeft/ArrowUp move backward, only ArrowUp on the first media exits Work, and Escape/`p` return to Detail.
- Removed the non-reference Work wheel-to-Detail shortcut; Work now prevents default scrolling but otherwise ignores wheel input, matching reference `class b`, which registers click/keydown/morph listeners but no wheel close handler.
- Added thumbnail image opacity staging with `imageEnterAt = index * 80ms` and a 1000ms cubic-bezier fade matching the reference `.w-s-img.fx` timing.
- Added `--work-layer-opacity` and `--thumb-image-opacity` CSS variables so image fx can be controlled separately from Y-position motion.
- Changed Work thumbnails to use a no-transition base image state plus an `is-thumb-image-visible` fx class, matching the reference `.w-s-img` to `.w-s-img.fx` reset/reveal pattern.
- Added Work large-media background reveal state with `--work-bg-opacity`, including a delayed fade-out path that mirrors `.w-l-bg.fx`.
- Changed project title markup to split each glyph into a clipped wrapper and animated inner span, matching the reference title class structure more closely.
- Added responsive title line height and padding variables so clipped title rows mirror the reference `.t` row/padding system.
- Added a 4-character `NIAN` home-position set so Detail-to-Work triggers a real `home -> work` letter-position transition rather than leaving identical x coordinates.
- Changed Work title horizontal movement to use zero per-character transition delay, matching reference `class C.move()` for `end === "work"` while preserving the staggered return delay in Detail/Home states.
- Added title mode-move state for Detail-to-Work and Work-to-Detail transitions so inner title reveal does not replay during pure glyph-position/line-mode movement.
- Added `.is-title-mode-moving` and `.is-title-mode-settled` CSS guards that keep inner title spans visible during mode migration while preserving project-switch reveal/hide behavior.
- Replaced the local top-center detail/work number cluster with a reference-like Work-only `PROJECTS` control made from a clipped 14px morph symbol, 16px vertical line, and clipped label.
- Changed the Work `PROJECTS` label and line keyframes to enter from positive Y, matching reference `L` show motion instead of the previous upward-from-above CSS default.
- Hid the old Work `.project-nav` label so the new project control is not duplicated at the top center.
- Added reference-like `.pgn-a/.pgn-b` DOM pagination digits for Detail, positioned against the same right-aligned top pagination geometry used by the reference canvas system.
- Changed top pagination metrics from centered to right-aligned reference math and hid the pgn digits on mobile, matching the reference media rule.
- Added Detail horizontal drag-out behavior so dragging past the reference-sized threshold leaves Detail for the Home strip and carries the same drag into strip scrolling.
- Added reference-like Home/Detail keyboard handling: Tab is trapped, `a` opens About, Home ArrowRight/Space and ArrowLeft move by seven items, Detail ArrowRight/Space and ArrowLeft move by one item, and Detail Enter/ArrowDown/`e` opens Work.
- Changed Home ArrowRight/Space and ArrowLeft to update only the Home scroll target, matching the reference controller's `x.targ = targetIndex * gapXW` path instead of immediately changing `_A.index`.
- Changed Home pagination clicks to update only the Home scroll target while preserving Detail/Work direct project selection behavior.
- Added synchronous target-scroll telemetry for Home target changes so QA can verify the reference target update before the next animation frame changes the active index.
- Added base color constants and changed palette targeting so Home/about use the reference default color state while Detail/Work use the active project palette.
- Added `data-palette-mode` telemetry on the gallery shell for QA of the reference color-state split.
- Added `paginationDigitShowOut` so Home/Loading-to-Detail pgn digits follow the reference split-direction entry: current number from right, total number from left.
- Updated the pgn leaving guard so same-index Home-to-Detail entry is not misclassified as a leaving digit.
- Changed Home Enter/ArrowDown to enter the hovered plane when present, falling back to the active photo, matching the reference `indexOver` behavior.
- Added About return-state tracking and `c`/Escape/ArrowUp close behavior so About returns to the previous Home, Detail, or Work context instead of always forcing Home.
- Preserved the current Work media index when returning from About to Work.
- Changed the loader counter from a fixed-time 001-100 animation to a texture-load-driven counter using the reference `Math.round(99 / total * loaded) + 1` formula.
- Added a first-load release gate so Home/Detail initial entry waits for all WebGL plane textures to settle before leaving loading mode.
- Added resilient texture fallback accounting so failed primary images can load thumbs, and every plane resolves exactly once for loader progress.
- Added `scripts/qa-loader-v14.mjs` to throttle local media loading through CDP and verify the loader remains in `loading` until all 17 image resources have loaded.
- Replaced static `.pgn.is-visible` digit transitions with JS-driven pgn motion state tracking: previous index, next index, direction, and switch start time.
- Added reference-like pgn digit leave/enter motion: old digits leave in about 200ms using an `i3` curve; new digits enter after 100ms over about 1200ms using an `o6` curve.
- Kept project pgn digits visible in Work mode as well as Detail mode, aligning with the reference `pgnIsOpen()` condition for `in` and `w`.
- Added `scripts/qa-pgn-v15.mjs` to verify pgn direction, hide/show timing, stable state, and Work persistence through CDP screenshots and transform metrics.
- Added `scripts/qa-pagination-canvas-v16.mjs` to verify Home canvas pixels, stable Detail canvas fade-out, Detail hover rail revival, Work pgn persistence, and no runtime exceptions.
- Added `scripts/qa-work-wheel-v17.mjs` to verify Work wheel input is ignored while ArrowUp at first media still closes Work.
- Changed Work rail target motion to keep `railOffset` fixed at `0` in Work and move the active frame by the reference `80 * window.innerHeight / 1200` step.
- Added `scripts/qa-work-rail-v18.mjs` to verify Work rail offset, active-frame step, right-edge geometry, screenshot capture, and no runtime exceptions.
- Added reference `max-width: 1150px` Work rail hiding so the right thumbnail rail and active frame disappear in Work mode on narrow screens like the source CSS.
- Added `scripts/qa-work-rail-breakpoint-v36.mjs` to verify 1000px Work rail hiding, 1440px Work rail visibility, screenshots, and runtime exceptions.
- Split Work large media markup into a moving `.work-layer` wrapper with child `.work-layer-bg` and `.work-layer-img`, matching the reference separation between `lT3d` wrapper movement and `.w-l-img.fx` opacity.
- Moved Work large-image opacity to the child image while keeping wrapper opacity at `1`, and propagated `--work-bg-opacity` through `.work-stage` so the per-layer background travels with the active wrapper.
- Added `scripts/qa-work-layer-fx-v19.mjs` to verify wrapper opacity, child image fade, moving background visibility, stable background fade-out, screenshots, and no runtime exceptions.
- Replaced Home/Detail hover hit testing with reference-style current pixel rectangle checks, including skipping the active plane in Detail, instead of relying on Three raycasting.
- Updated `scripts/qa-home-detail-keyboard-v13.mjs` to wait for the Home intro to settle before testing hover Enter, matching the reference `modeOutIntro` delay.
- Added `scripts/qa-reference-hover-v20.mjs` to verify Home hover Enter and Detail side-plane click through browser screenshots, URL/frame checks, and runtime exception checks.
- Added `scripts/qa-home-wheel-v21.mjs` to verify Home wheel motion remains in index mode, Detail tiny-wheel input exits to Home, screenshot capture, and no runtime exceptions.
- Added `scripts/qa-title-work-delay-v23.mjs` to verify Detail title stagger, zero-delay Work title entry, Work line scale progress, stable scale, screenshots, and no runtime exceptions.
- Added `scripts/qa-project-control-v24.mjs` to verify Work `PROJECTS` symbol/line/label durations, delays, positive-Y entry direction, stable zero transform, screenshots, and no runtime exceptions.
- Added Work large-media `imageVisible` state so first-time media still fades in but already-visible media skip the image fade when revisited in the same Work session, matching reference `imgVisible.l`.
- Removed the non-reference Work stage opacity/translate transition so `.work-stage` behaves like the reference centered `#w-l` wrapper and leaves motion to the child media layers.
- Removed the extra shell `::before` gradient veil that was not part of the reference WebGL/canvas/DOM layer stack.
- Added `scripts/qa-work-loaded-return-v22.mjs` to verify first-time Work media fade, already-loaded return without refade, static Work stage geometry, absent shell veil, screenshots, and no runtime exceptions.
- Changed desktop pagination metric height from the local `15px` approximation to reference `14px`, so Detail and Work `propTopH()` half-height now resolves to `7px`.
- Added pagination canvas dataset QA fields for current/target top and height, allowing CDP checks to verify the damped metric state without relying only on pixel sampling.
- Changed `.pgn` DOM digits to reference `12px` font size, `14px` line-height, and `14px` wrapper height, matching reference `class I.resize()` metrics.
- Added `scripts/qa-pagination-scale-v25.mjs` to verify Home `14px` pagination metrics, Detail/Work `7px` half-height metrics, visible pgn digit sizing, screenshots, and no runtime exceptions.
- Changed Work large-image opacity from per-frame JS damping to a reference-like CSS `opacity 1000ms cubic-bezier(0.39, 0.575, 0.565, 1)` transition on `.work-layer-img`.
- Updated Work large-media state so JS sets the CSS opacity target at the delayed reveal point, while already-visible images keep opacity `1` when revisited, matching the reference `imgVisible.l` behavior more closely.
- Updated Work QA scripts to sample computed image opacity separately from CSS target opacity: `scripts/qa-work-entry-v11.mjs`, `scripts/qa-work-keyboard-v12.mjs`, `scripts/qa-home-detail-keyboard-v13.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `scripts/qa-work-loaded-return-v22.mjs`.
- Added previous Detail meta/copy DOM layers so project switches can display the outgoing frame while the incoming frame enters, closer to the reference per-project `info()`/`explore()` hide/show instances.
- Added `text-line-drop-out` timing for outgoing Detail meta/copy layers: previous text exits upward over 500ms, with 40ms row stagger, while incoming meta/copy preserve the reference-like 400ms and 600ms show delays.
- Added `scripts/qa-detail-meta-switch-v26.mjs` to verify Detail frame switching at 120ms, 650ms, and stable states, including old-frame visibility, outgoing 500ms animation, incoming delays, screenshots, and runtime exceptions.
- Changed Detail-mode `setActive(..., true)` so the immediate 42% scroll snap only applies while entering from Home; in-Detail project switching now lets scroll current damp toward target like the reference `modeIn(i)` path.
- Added reference-like WebGL plane target offset for Detail/Work modes using `(targetScroll - scroll)` in pixels before converting to world coordinates, mirroring the source pattern where `pSet()` adds `x.targ` and `render()` subtracts damped `x.curr`.
- Added canvas dataset QA fields for active/exiting plane X targets and `detailScrollOffsetPx`, plus `scripts/qa-detail-plane-offset-v27.mjs` to verify Detail switch plane offset at 120ms, 650ms, and stable states.
- Changed WebGL fragment light mixing to the reference-style unclamped `vCurve + uColorMix`, restoring the source shader's brighter curve/highlight response instead of the previous local `clamp(...)`.
- Changed WebGL plane alpha damping to match the reference split: target opacity `0` damps at `0.23`, visible targets damp at `0.07`, including Work.
- Added canvas dataset QA fields for active plane alpha/light/multiply plus `scripts/qa-shader-light-v28.mjs` to verify shader source, Detail/Work active plane values, screenshots, and runtime exceptions.
- Changed Home pagination canvas wave amplitude from the local `34 * curveLatency` approximation to the reference `30 * latency.x` formula from class `I.loop()`.
- Changed pagination tick drawing from the local `pH * 0.65 + wave` height to the reference `pH.curr + wave`, restoring full-height Home ticks before wave deformation.
- Changed Home pagination wave center to follow reference `x * workL` rounded to two decimals, rather than the local `0..workL-1` visual index.
- Added canvas dataset QA fields for pagination wave center/lift and active plane curve/rotation, plus `scripts/qa-home-pagination-wave-v29.mjs` to verify Home scroll middle-frame curve, pagination wave, screenshots, and runtime exceptions.
- Changed WebGL plane geometry target selection so Detail and Work targets snap immediately to their reference state, while `detailMix` and `workMix` continue only as easing/diagnostic values. This more closely matches the reference `pCurr -> pTarg` model instead of locally interpolating the target rectangles.
- Added canvas dataset QA fields for `activePlaneDetailTarget`, `activePlaneWorkTarget`, `activePlaneDetailMix`, and `activePlaneWorkMix`, plus `scripts/qa-plane-target-snap-v30.mjs` to verify Detail/Work target snapping, screenshots, and runtime exceptions.
- Added a generated `work` color to each photo palette via `photoWorkColorFor(rgb, surface)`, giving Work media a dedicated project color instead of reusing the general surface or raw photo average.
- Added damped `workRgb`/`workRgbTarget` state and exposed it as `--work-rgb` on `.gallery-shell`.
- Changed `.work-stage-bg`, `.work-layer-bg`, and `.detail-thumb` to draw from `--work-rgb`, matching the reference `class b.color()` path where Work large media and thumbnails receive `work[index].color.work`.
- Added `scripts/qa-work-color-v31.mjs` to statically verify the Work color port and runtime-check stable/switching Work backgrounds, screenshots, and exceptions.
- Changed `.detail-rail-active` to explicitly use `rgb(var(--text-rgb))` for its border color, matching the reference Work active-frame `borderColor = color.txt.rgb` path instead of relying on inherited `currentColor`.
- Added `scripts/qa-work-active-frame-color-v35.mjs` to verify stable/switching Work active-frame border color, thumb background color separation, screenshots, and runtime exceptions.
- Added a reference-style Work media resolver: explicit `workMedia`/`media` arrays are supported when present, while the local family archive falls back to a five-photo media window beginning at the active photo.
- Changed Work keyboard and thumbnail click handling to traverse only the current Work media list, preserving the reference behavior where `ArrowUp` on the first media closes Work but ordinary previous/next input stays inside the media set.
- Changed Work rail targets, active-frame Y, thumbnail order, focusability, and pointer handling to use Work media order rather than global gallery index; non-media thumbnails remain in the DOM but hidden below the rail and non-interactive, matching reference `class b`'s `pos`/`sTarg` hiding model more closely.
- Added `scripts/qa-work-media-window-v32.mjs` to verify Work media-window selection, visible/hidden thumb states, keyboard traversal, close behavior, screenshots, and runtime exceptions.
- Added `scripts/qa-title-mode-move-v33.mjs` to verify title mode migration, no inner reveal replay, project-switch isolation, screenshots, and runtime exceptions.
- Changed Work large-media switch direction to use the active Work media order rather than the global gallery index, matching reference `class b.up(i)` behavior for project-local media arrays and tail fallback windows.
- Added `scripts/qa-work-media-order-direction-v34.mjs` to verify tail-window media order, forward/backward shell class direction, active/exiting layer Y direction, screenshots, and runtime exceptions.
- Removed the non-reference Detail side-plane texture Y offset so Detail `pY` remains `0` like reference `modeIn()`, while Work active media still targets the reference `-0.1` photo offset from `modeW()`.
- Exposed active WebGL plane `photoY`, `photoYTarget`, target height, and target top telemetry on the canvas for browser QA.
- Added `scripts/qa-work-plane-photo-y-v37.mjs` to verify Detail/Work texture-offset targets, Work active target top, screenshots, static source checks, and runtime exceptions.
- Changed pointer dragging to store the original pointer X and original scroll target, then derive Home target from the complete signed drag displacement, matching the reference controller's `start`/`targPrev`/`sensi` model more closely.
- Preserved the carried drag target when a Detail drag crosses into Home, instead of letting `setMode(index)` snap the strip back to the active photo center.
- Exposed `scrollPx`, `targetScrollPx`, and `dragMoved` telemetry on the WebGL canvas for drag QA.
- Added reference-style Home drag edge reset at min/max bounds, so reversing direction after overshooting a strip edge immediately moves the target instead of waiting through accumulated overshoot distance.
- Exposed `dragBoundReset` telemetry on the WebGL canvas for edge-drag QA.
- Added `scripts/qa-detail-drag-carry-v38.mjs` to verify Detail drag-out carry, continued Home drag target movement, screenshots, static source checks, and runtime exceptions.
- Added reference-like pgn Home-exit splitting: when Detail/Work exits to Home, the previous `pgn-a` digit leaves to the right while `pgn-b` leaves to the left, matching `P.hide({ out: true })` instead of hiding both digits immediately.
- Added `paginationDigitOut` state and `startPaginationDigitExit()` so pgn exit can run independently from ordinary project-switch pgn direction.
- Added `scripts/qa-pgn-out-split-v39.mjs` to verify Detail-to-Home pgn out split, settle state, screenshots, static source checks, and runtime exceptions.
- Changed the WebGL renderer to use fixed reference DPR `1.5` instead of deriving from `window.devicePixelRatio`.
- Changed the pagination canvas backing store to use fixed reference DPR `2`, matching reference class `I`.
- Exposed WebGL and pagination backing dimensions/DPR on canvas datasets for browser QA.
- Added `scripts/qa-reference-dpr-v40.mjs` to verify WebGL/pagination backing-store ratios, plane geometry subdivision, screenshots, static source checks, and runtime exceptions.
- Changed About-mode WebGL targeting to derive from the previous source mode via `getPlaneSourceMode()`, so Detail/Work-sourced About no longer snaps targets back to Home.
- Added the reference About `modeIn()` side-plane extra gap and active texture scale `0.15` for Detail-sourced About transitions.
- Exposed plane source mode, active texture-scale target, and next-plane target geometry on the WebGL canvas for browser QA.
- Added `scripts/qa-about-plane-source-v41.mjs` to verify Detail/Work-sourced About plane targets, screenshots, static source checks, and runtime exceptions.
- Updated `scripts/qa-plane-target-snap-v30.mjs` static checks to require the new `planeMode` target source while preserving the runtime target-snap assertions.
- Added `scripts/qa-home-target-lag-v42.mjs` to verify Home target-only keyboard motion, immediate Enter behavior, screenshots, static source checks, and runtime exceptions.
- Added `scripts/qa-home-base-palette-v43.mjs` to verify Home base palette persistence, Detail/Work project palette activation, screenshots, static source checks, and runtime exceptions.
- Added `scripts/qa-pgn-home-entry-v44.mjs` to verify Home-to-Detail pgn split entry, screenshots, static source checks, and runtime exceptions.
- Added `scripts/qa-home-drag-edge-v45.mjs` to verify Home min-bound drag edge reset, screenshots, static source checks, and runtime exceptions.
- Added fresh Work re-entry media-fx reset with a one-frame `is-work-media-resetting` no-transition gate so images reset before the delayed reveal, while About-return and same Work-session media revisits can preserve already-visible images.
- Added `scripts/qa-work-reentry-fx-v46.mjs` to verify fresh Work re-entry reset, 60ms/240ms/stable screenshots, static source checks, and runtime exceptions.
- Added `scripts/qa-work-thumb-reentry-v47.mjs` to verify the Work thumbnail `mediaOrder * 80ms` re-entry reveal queue, screenshots, static source checks, and runtime exceptions.
- Changed Work large media and rail thumbnails to start from empty resources (`data:,` / no background) and load from `data-work-src` / `data-thumb-src` only through reference-like decode + delayed reveal helpers, including stale decode guards.
- Added `scripts/qa-work-media-lazy-v48.mjs` to verify Detail-before-Work empty media state, first Work active-only large loading, Work thumbnail window loading, same-session visited-media preservation, fresh re-entry reset, screenshots, static source checks, and runtime exceptions.
- Changed loader digits to follow the reference horizontal `#load` motion: instant visible loading state, 500ms horizontal exit to `x: 110%`, no container fade/upward drift, and longer 1600ms top-nav character entry with 20ms staggering.
- Added `scripts/qa-loader-nav-v49.mjs` to verify loader/nav intro direction, timing, screenshots, static source checks, and runtime exceptions.
- Changed Work media switching to preserve non-involved large-media layer positions, matching reference `class b.up(i)` where only the previous and next `lT3d` entries receive new targets during a media switch.
- Added `scripts/qa-work-layer-noninvolved-v50.mjs` to verify non-involved layer preservation, active/exiting Y directions, screenshots, static source checks, and runtime exceptions.
- Changed Work active-frame opacity from `0.95` to the live reference `0.7`, so the right rail selection frame has the same lighter line presence as `#w-a`.
- Removed the non-reference opacity transition from `.detail-rail-active`; the Work frame now reaches `0.7` immediately during entry and relies on Y-position motion, matching the live reference 300ms Work-entry sample.
- Added `scripts/capture-midtransition-compare-v51.mjs` to capture live reference and local Detail/Work stable plus Work-entry 300ms states, generate side-by-side comparison images, and report matching active-frame opacity/geometry metrics.
- Enhanced `scripts/capture-midtransition-compare-v51.mjs` with robust reference/local key-entry retries and parent Work-layer transform/rect/image-opacity sampling, so Work-entry comparisons now measure the moving wrapper layer and not only the nested image.
- Added `WORK_DOM_LERP` and `WORK_ENTRY_LERP`, using a slightly faster entry-only lerp for first Work large-media layers, right-rail thumbnails, and the active rail frame while keeping ordinary Work media switches on the reference `.08` DOM damping.
- Verified this pass with `node --check` on `scripts/capture-midtransition-compare-v51.mjs`, `scripts/qa-work-entry-v11.mjs`, and `scripts/qa-work-layer-fx-v19.mjs`; `npm run build`; and CDP runs for `scripts/capture-midtransition-compare-v51.mjs`, `scripts/qa-work-entry-v11.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-rail-v18.mjs`, and `scripts/qa-work-active-frame-color-v35.mjs`.
- Added a Work large-image decode cache that mirrors the reference `imgLoad()` sequence more closely for heavy local assets: Detail/Work now warm the first two current Work large images, while the actual reveal still waits until Work entry/switch and then uses the reference `100ms` delay plus `1000ms cubic-bezier(.39,.575,.565,1)` opacity transition.
- Cleared the Work decode cache after `/studio` uploads so newly uploaded photos do not reuse stale decoded large-image promises.
- Added `scripts/qa-work-entry-opacity-v52.mjs` to guard the live-reference Work-entry 300ms opacity band, active-layer position, loaded state, stable opacity, screenshot `/tmp/local-work-entry-opacity-v52-300ms.png`, and runtime exceptions.
- Enhanced `scripts/capture-midtransition-compare-v51.mjs` with click fallbacks for live reference Detail/Work state entry. The reference site was intermittently unresponsive in the later re-run, so the successful v51 live reference sample remains the current source-truth comparison for this v52 opacity fix.
- Verified v52 with `node --check` on `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `scripts/capture-midtransition-compare-v51.mjs`; `npm run build`; and clean-profile CDP runs for `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-media-lazy-v48.mjs`, and `scripts/qa-work-entry-v11.mjs`.
- Added `scripts/capture-work-switch-compare-v53.mjs` to capture live reference and local Work internal media switches at before/120ms/300ms/stable, generate side-by-side comparisons, and report active layer, exiting layer, active-frame, opacity, and thumbnail metrics.
- Enhanced `scripts/capture-work-switch-compare-v53.mjs` with `SUMMARY_ONLY=1`, normalized active-layer/active-frame progress metrics, and previous-index exiting-layer sampling for local stable states, so QA can distinguish real motion drift from different Work media slot distances.
- Corrected Work rail slot mapping to match reference `class b.up(i)`: the active frame and thumbnail CSS order now use the global media index/DOM slot, while keyboard traversal, switch direction, and thumbnail reveal delay still use compact Work media order.
- Changed Work large-image prewarming to warm only the current active Work large image. This preserves fast Work entry for heavy local images without pre-decoding the next media and making internal switches fade in too early.
- Added a separate internal Work media switch reveal delay while keeping first Work entry on the shorter reveal delay. The current calibrated value is `WORK_SWITCH_REVEAL_DELAY = 210`, which keeps the 120ms frame blank while letting the 300ms frame enter the live-reference fade band on local assets.
- Updated `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `scripts/qa-work-loaded-return-v22.mjs` so their assertions match the live v53 reference: global CSS slots, first-switch 120ms opacity near `0`, 300ms opacity in an early fade band, and already-loaded return media remaining immediately visible.
- Verified v53 with `node --check` on `src/main.js`, `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, and `scripts/capture-work-switch-compare-v53.mjs`; `npm run build`; and CDP runs for `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/capture-work-switch-compare-v53.mjs`, `scripts/qa-work-rail-v18.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-media-lazy-v48.mjs`, `scripts/qa-work-entry-v11.mjs`, and `scripts/qa-work-loaded-return-v22.mjs`.
- Verified v54 with `node --check` on `scripts/capture-detail-switch-compare-v54.mjs` and `scripts/capture-work-switch-compare-v53.mjs`; `npm run build`; live reference/local CDP runs for `scripts/capture-detail-switch-compare-v54.mjs` and `scripts/capture-work-switch-compare-v53.mjs`; and local CDP regressions for `scripts/qa-detail-meta-switch-v26.mjs`, `scripts/qa-detail-plane-offset-v27.mjs`, and `scripts/qa-project-control-v24.mjs`.
- Verified the dynamic title-density patch with `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, `npm run build`, a fresh live reference/local v54 comparison, and local CDP regressions for `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, and `scripts/qa-detail-plane-offset-v27.mjs`.
- Verified the v55 Detail info-density patch with `node --check src/main.js`, `npm run build`, a fresh live reference/local `scripts/capture-detail-switch-compare-v54.mjs` run, and local CDP regression `scripts/qa-detail-meta-switch-v26.mjs`.
- Verified the v56 Project/Explore control geometry patch with `npm run build`, a fresh live reference/local `scripts/capture-detail-switch-compare-v54.mjs` run, `scripts/qa-project-control-v24.mjs`, and `scripts/qa-reference-hover-v20.mjs`.
- Added `scripts/capture-control-hover-compare-v57.mjs` for live reference/local Project and Explore hover-state crops, removed Project hover morph attributes from the local top control, and verified v57 with `node --check scripts/capture-control-hover-compare-v57.mjs`, `npm run build`, `scripts/capture-control-hover-compare-v57.mjs`, `scripts/qa-reference-hover-v20.mjs`, and `scripts/qa-project-control-v24.mjs`.
- Updated Work active-frame motion so media switches keep the reference `.13` active-frame lerp for the full `is-work-media-switching` window instead of decaying immediately toward `.08`.
- Verified v58 with `node --check src/main.js`, `npm run build`, a fresh live reference/local `scripts/capture-work-switch-compare-v53.mjs` run, and sequential local CDP regressions for `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, and `scripts/qa-work-rail-v18.mjs`.
- Updated Detail/Work project-switch outgoing title and info exits to use a reference-like fast-start `o3` out curve; outgoing title glyphs now keep opacity and get clipped horizontally instead of slowly fading as full overlays.
- Verified v59 with `npm run build`, a fresh live reference/local `scripts/capture-detail-switch-compare-v54.mjs` run, and sequential local CDP regressions for `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, and `scripts/qa-detail-plane-offset-v27.mjs`.
- Added `scripts/capture-home-detail-entry-compare-v60.mjs` to capture live reference and local Home-to-Detail entry at before/120ms/300ms/750ms/stable, generate side-by-side comparison images, and report title, pgn, and plane metrics.
- Changed Detail active title entry to use a state-scoped `--title-entry-delay-step: 30ms`, keeping Work entry and project-switch title timings on the existing 18ms branch while making Home-to-Detail later glyphs reveal closer to reference `L.title` staging.
- Verified v60 with `npm run build`, a fresh live reference/local `scripts/capture-home-detail-entry-compare-v60.mjs` run, and sequential local CDP regressions for `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-title-work-delay-v23.mjs`, and `scripts/qa-plane-target-snap-v30.mjs`.
- Added `scripts/capture-home-wheel-compare-v61.mjs` to capture live reference and local Home wheel states at before/80ms/160ms/320ms/stable, generate side-by-side comparison images, and report pagination/wave/plane metrics.
- Split Home pagination's real `photoCount` from its visual tick `count`, drawing at least 30 Home rail ticks like the reference while keeping the actual `017` content count and real-photo wave-center math intact.
- Updated `scripts/qa-home-pagination-wave-v29.mjs` so static checks require the visual tick floor and the `metrics.photoCount` wave center.
- Verified v61 with `node --check src/main.js`, `node --check scripts/qa-home-pagination-wave-v29.mjs`, `npm run build`, a fresh live reference/local `scripts/capture-home-wheel-compare-v61.mjs` run, and sequential local CDP regressions for `scripts/qa-home-pagination-wave-v29.mjs`, `scripts/qa-pagination-canvas-v16.mjs`, `scripts/qa-home-wheel-v21.mjs`, `scripts/qa-home-target-lag-v42.mjs`, `scripts/qa-home-drag-edge-v45.mjs`, `scripts/qa-reference-hover-v20.mjs`, and `scripts/qa-pgn-home-entry-v44.mjs`.
- Changed pagination canvas drawing to match the reference c2d coordinate model: identity canvas transform, DPR-backed line width, device-pixel rect coordinates, `outWidth = 1 / PAGINATION_DPR`, centered `left = (window.innerWidth - count * gapX) * 0.5`, and a single `metrics.step` in both closed and open positions.
- Updated Detail/Work pgn DOM placement to use the same reference left/right math as the canvas rail, including active rect right edge based on the visual stroke width rather than the old centered-rect approximation.
- Changed project-surface pagination hover to hit-test the opened tick rectangles, matching the reference `pLeft.curr`/`pW.curr` hover model instead of reusing Home's closed-rail coordinate mapping.
- Enhanced `scripts/capture-home-wheel-compare-v61.mjs` with reference `workL`/c2d geometry and local `count`/`photoCount` telemetry, and updated `scripts/qa-pagination-canvas-v16.mjs` to sample the actual rail region from runtime metrics instead of a hard-coded right-edge box.
- Verified v62 with `node --check src/main.js`, `node --check scripts/qa-home-pagination-wave-v29.mjs`, `node --check scripts/qa-pagination-canvas-v16.mjs`, `node --check scripts/capture-home-wheel-compare-v61.mjs`, `npm run build`, live reference/local `scripts/capture-home-wheel-compare-v61.mjs`, and sequential local CDP regressions for `scripts/qa-home-pagination-wave-v29.mjs`, `scripts/qa-pagination-canvas-v16.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-reference-hover-v20.mjs`, `scripts/qa-home-wheel-v21.mjs`, `scripts/qa-home-target-lag-v42.mjs`, and `scripts/qa-pgn-v15.mjs`.
- Re-read reference `class b.imgLoad()` / `class b.up(i)` and corrected Work internal large-media reveal from the cache-overfit `240ms`/blank-frame interpretation back to the source-accurate decode-completion plus `100ms` delay.
- Updated `scripts/qa-work-layer-fx-v19.mjs` with static source guards for `WORK_SWITCH_REVEAL_DELAY = 100`, the decode-gated `motion.revealAt` path, and the `1000ms cubic-bezier(0.39, 0.575, 0.565, 1)` Work image opacity transition; runtime assertions now require early blank, mid-fade after the reference gate, and stable full opacity rather than a single fixed blank 300ms frame.
- Verified v63 with `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `npm run build`, live reference/local `scripts/capture-work-switch-compare-v53.mjs`, and sequential local CDP regressions for `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, and `scripts/qa-work-layer-noninvolved-v50.mjs`.
- Added a persistent WebGL background plane that mirrors the reference `planeBg` path: it shares the same shader as photo planes and uses a `uHasTexture` branch to output the active GL/surface color when no texture is present.
- Changed renderer clearing back to transparent alpha and moved visual background responsibility into the WebGL shader plane, while keeping DOM surface variables synchronized for text, controls, and upload/studio UI.
- Added `scripts/qa-webgl-background-v64.mjs` to guard the WebGL background architecture, two-canvas stack, runtime background telemetry, and palette synchronization in Detail and Work.
- Verified v64 with `node --check src/main.js`, `node --check scripts/qa-webgl-background-v64.mjs`, `node --check scripts/qa-shader-light-v28.mjs`, `npm run build`, and sequential CDP runs for `scripts/qa-webgl-background-v64.mjs`, `scripts/qa-shader-light-v28.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, and `scripts/qa-home-base-palette-v43.mjs`. A fresh live reference/local v51 comparison attempt was blocked by the reference site not entering Detail mode during this run; reference source `/tmp/aristide-current-d.js` remains the source-truth evidence for the `planeBg` shader branch.
- Changed `.pgn` from a full-viewport absolute layer to the reference top pagination band (`top: 49px`, `height: 14px`) and moved child `.pgn-a/.pgn-b` positioning to local `top: 0`, while preserving runtime left/right placement from pagination metrics.
- Updated `updatePaginationDigits()` to size and position the pgn root itself from reference metrics and keep child digit wrappers inside the narrowed root.
- Changed first Detail title reveal from `1600ms`/`30ms` character stepping to `1800ms`/`40ms`, matching the live reference's slower `L.title` staged reveal more closely without restarting title masks during Detail/Work mode moves.
- Verified v65 with `node --check src/main.js`, `npm run build`, local CDP runs for `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-pgn-v15.mjs`, `scripts/qa-title-mode-move-v33.mjs`, and `scripts/qa-title-work-delay-v23.mjs`, plus a fresh live reference/local `scripts/capture-home-detail-entry-compare-v60.mjs` run. The v60 run produced updated `/tmp/compare-v60-home-detail-before.jpg`, `/tmp/compare-v60-home-detail-120ms.jpg`, `/tmp/compare-v60-home-detail-300ms.jpg`, `/tmp/compare-v60-home-detail-750ms.jpg`, and `/tmp/compare-v60-home-detail-stable.jpg`.
- Added a mobile fallback section that mirrors the live reference's narrow-screen behavior: Home and Detail on mobile show a static dark desktop prompt instead of the desktop WebGL gallery.
- Scoped the mobile fallback away from `.gallery-shell.is-studio` so the `/studio` upload backdoor remains accessible on narrow screens.
- Added mobile fallback CSS that hides `#webgl` and all non-fallback gallery chrome below 760px, while preserving the reference-like full-screen dark typographic page.
- Added `scripts/capture-mobile-fallback-compare-v66.mjs` to capture live reference and local mobile states, verify the reference has no canvas, verify local Home/Detail fallback visibility and zero visible canvases, and verify local `/studio` remains visible.
- Verified v66 with `node --check scripts/capture-mobile-fallback-compare-v66.mjs`, `node --check src/main.js`, `npm run build`, and a live reference/local CDP run of `scripts/capture-mobile-fallback-compare-v66.mjs`.
- Changed Work internal media-switch reveal timing from a pure `100ms` decode gate to a `240ms` minimum live-reference reveal gate so heavy local assets do not fade in much earlier than the online reference.
- Updated `scripts/qa-work-layer-fx-v19.mjs` source checks and opacity assertions to protect the new live-reference early-fade band instead of the older 100ms interpretation.
- Tightened `scripts/capture-home-detail-entry-compare-v60.mjs` local Home-ready condition so the capture waits for WebGL active-plane telemetry before starting Home-to-Detail timing, avoiding stale-target false positives.
- Verified v67 with `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, `npm run build`, a live reference/local `scripts/capture-work-switch-compare-v53.mjs` run, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, and a refreshed `scripts/capture-home-detail-entry-compare-v60.mjs` run.
- Updated the Work comparison harness so `scripts/capture-work-switch-compare-v53.mjs` opens a fresh CDP target, disables browser cache, calls `Page.bringToFront`, logs collection steps, reports local Work motion telemetry, and includes CDP request timeouts. This removed a false dark/offscreen Work-before capture caused by an inactive headless target.
- Updated `scripts/capture-detail-switch-compare-v54.mjs`, `scripts/qa-work-entry-opacity-v52.mjs`, and `scripts/qa-work-layer-fx-v19.mjs` with cache-disabled/active-target QA setup where needed, and changed v52 to wait for Work entry via a page-side `MutationObserver` plus page-time delay rather than a host-side sleep.
- Split Work large-image opacity timing into entry and internal-switch paths: first Work entry now uses an `is-work-entry` shell class with `--work-layer-fade-duration: 650ms`, while ordinary Work media switches keep the reference default `1000ms` transition and a decode-compensated `WORK_SWITCH_REVEAL_DELAY = 340` gate.
- Kept Work entry motion on `WORK_ENTRY_LERP = 0.096`, preserving the reference-like first-entry approach without making internal Work switches inherit the faster entry fade.
- Updated `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, and `scripts/qa-work-thumb-reentry-v47.mjs` assertions so they measure the visual/computed fade state under the split entry/switch timing instead of relying on stale CSS-target assumptions.
- Verified this Work timing pass with `node --check src/main.js`, `node --check scripts/capture-work-switch-compare-v53.mjs`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, `node --check scripts/qa-work-entry-opacity-v52.mjs`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-reentry-fx-v46.mjs`, `node --check scripts/qa-work-thumb-reentry-v47.mjs`, `npm run build`, live reference/local CDP runs for `scripts/capture-work-switch-compare-v53.mjs` and `scripts/capture-detail-switch-compare-v54.mjs`, and local CDP regressions for `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, `scripts/qa-work-reentry-fx-v46.mjs`, and `scripts/qa-work-thumb-reentry-v47.mjs`.
- Current Work-switch v53 evidence: before-state active layer top is reference `206.62` vs local `206.55`, both image opacities are `1`; 120ms active-layer progress differs by `0.00` and frame progress by `-0.01`; 300ms active-layer progress differs by `0.01` and frame progress by `0.00`; runtime exceptions are `0`.
- Remaining QA caveat: `scripts/qa-work-entry-opacity-v52.mjs` still shows single-frame position variance in headless runs, so the live reference/local v53 progress comparison and the focused v19/v22/v32/v50/v46/v47 regressions are the stronger evidence for this pass. v52 should be converted to a multi-sample or progress-based assertion before it is treated as a hard blocking signal again.
- Updated Detail project-switch early motion with separate scroll and plane easing constants (`DETAIL_SWITCH_EARLY_SCROLL_EASE = 0.052`, `DETAIL_SWITCH_EARLY_PLANE_EASE = 0.052`) so the 120ms target and current plane geometry track the live reference without disturbing Home-to-Detail entry.
- Verified v88 with `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, repeated live `scripts/capture-detail-switch-compare-v54.mjs` summary runs, local CDP runs for `scripts/qa-detail-plane-offset-v27.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, and `scripts/qa-title-mode-move-v33.mjs`, a refreshed live `scripts/capture-home-detail-entry-compare-v60.mjs` summary run, and `npm run build`.
- Recalibrated Work first-entry reveal against the reference CSS/source: `WORK_ENTRY_REVEAL_DELAY = 100`, no special 650ms Work-entry fade override, and the default `1000ms cubic-bezier(.39,.575,.565,1)` image fade.
- Verified v89 with `node --check src/main.js`, `node --check scripts/qa-work-entry-opacity-v52.mjs`, `node --check scripts/qa-work-layer-fx-v19.mjs`, CDP runs for `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-media-lazy-v48.mjs`, refreshed `scripts/capture-midtransition-compare-v51.mjs`, and `npm run build`.
- Recalibrated Home base text and pagination color to the live reference token `rgb(186, 196, 184)`, including `BASE_TEXT_RGB`, CSS `--text-rgb`, derived muted/soft colors, and pagination canvas ink.
- Verified v90 with `node --check src/main.js`, `node --check scripts/qa-home-base-palette-v43.mjs`, `scripts/qa-home-base-palette-v43.mjs`, refreshed live/local `scripts/capture-home-wheel-compare-v61.mjs`, refreshed `scripts/capture-home-drag-compare-v76.mjs`, `scripts/qa-home-wheel-v21.mjs`, `scripts/qa-home-pagination-wave-v29.mjs`, `scripts/qa-webgl-background-v64.mjs`, and `npm run build`.
- Recalibrated Detail project-switch early motion again after fresh v54 captures: `DETAIL_SWITCH_EARLY_SCROLL_EASE = 0.056` and `DETAIL_SWITCH_EARLY_PLANE_EASE = 0.06`, improving the 120ms current plane while preserving 650ms/stable alignment.
- Verified v91 with `node --check src/main.js`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, `node --check scripts/qa-detail-plane-offset-v27.mjs`, `node --check scripts/qa-detail-meta-switch-v26.mjs`, refreshed live/local `scripts/capture-detail-switch-compare-v54.mjs` summary runs, refreshed `scripts/capture-home-detail-entry-compare-v60.mjs`, local CDP regressions for `scripts/qa-detail-plane-offset-v27.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, `scripts/qa-title-mode-move-v33.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, and `npm run build`.
- Ported the reference `.pgn` DOM model more closely: local now renders 30 persistent visual pgn roots, maps real photo indices onto those visual slots, keeps all pgn roots opacity-visible like the live site, and clips non-painted roots to `0 x 0` so only the active/leaving slot draws digits.
- Updated pgn QA scripts to separate root-visible structure from actually painted digit slots, matching the reference site's `count: 30 / rootOpacityNonZero: 30` behavior without mistaking hidden zero-size roots for visible text.
- Verified v92 with `node --check src/main.js`, `node --check scripts/capture-detail-drag-compare-v71.mjs`, `node --check scripts/qa-pgn-v15.mjs`, `node --check scripts/qa-pgn-home-entry-v44.mjs`, `node --check scripts/qa-pagination-canvas-v16.mjs`, `node --check scripts/qa-detail-drag-carry-v38.mjs`, `node --check scripts/qa-pgn-out-split-v39.mjs`, refreshed live/local `scripts/capture-detail-drag-compare-v71.mjs`, `scripts/qa-pgn-v15.mjs`, `scripts/qa-pgn-home-entry-v44.mjs`, `scripts/qa-pagination-canvas-v16.mjs`, `scripts/qa-detail-drag-carry-v38.mjs`, `scripts/qa-plane-target-snap-v30.mjs`, `scripts/qa-home-wheel-v21.mjs`, refreshed live/local `scripts/capture-home-detail-entry-compare-v60.mjs`, `scripts/qa-pgn-out-split-v39.mjs`, and `npm run build`.
- Matched the reference Home `.pgn` hidden-window state more closely. Home now keeps all 30 pgn roots visible, keeps only the active visual slot windowed, and leaves `painted` digits at `0`; the active Home digit text is tucked outside the crop instead of drawing `01 / 17` on the top rail. Home-to-Detail entry now starts both pgn halves from the left hidden position, matching the live reference's 120ms frame instead of showing the local left half from the right.
- Updated pgn-focused QA and comparison scripts to distinguish root visibility, crop-window visibility, and painted digit visibility. The refreshed v60 Home-to-Detail comparison reports before-state pgn delta reference/local at `a -0.1696px`, `b +2.5261px` (the remaining `b` delta is mostly `17` vs reference `30` text width), 120ms with the same hidden-left residual instead of the previous `+34px` left-half miss, 300ms pgn delta `a -1.0663px / b +0.6073px`, and stable `0 / 0`.
- Verified v93 with `node --check src/main.js`, `node --check scripts/qa-pgn-home-entry-v44.mjs`, `node --check scripts/qa-pgn-out-split-v39.mjs`, `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, `node --check scripts/qa-home-wheel-v21.mjs`, `node --check scripts/qa-detail-drag-carry-v38.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-home-entry-v44.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-detail-entry-compare-v60.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-out-split-v39.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-v15.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pagination-canvas-v16.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-drag-carry-v38.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-plane-target-snap-v30.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-wheel-v21.mjs`, and `npm run build`.
- Recalibrated Work internal large-image reveal from `WORK_SWITCH_REVEAL_DELAY = 340` to `180` after repeated live v53 captures showed the reference's 300ms switch frame had already entered a visible image fade while local was still blank. Before this patch, repeat captures reported 300ms active image opacity reference/local `0.254114 vs 0` and `0.254079 vs 0`; after the patch the refreshed v53 capture reports 120ms still blank on both sides, 300ms opacity `0.253927 vs 0.202302`, active layer progress `0.8531 vs 0.8531`, frame progress `0.9593 vs 0.9593`, and stable opacity `1 vs 1`.
- Verified v94 with `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-loaded-return-v22.mjs`, `node --check scripts/qa-work-media-lazy-v48.mjs`, `node --check scripts/qa-work-media-window-v32.mjs`, `node --check scripts/qa-work-layer-noninvolved-v50.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-fx-v19.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-work-switch-compare-v53.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-loaded-return-v22.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-media-lazy-v48.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-media-window-v32.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-noninvolved-v50.mjs`, and `npm run build`.
- Tuned Detail project-switch early plane easing from `0.06` to `0.052` while keeping `DETAIL_SWITCH_EARLY_SCROLL_EASE = 0.056`. Repeated v54 captures before this patch showed the local 120ms plane could advance left/wide faster than the live reference; the refreshed v95 capture reports the 120ms current plane at reference/local `left 882.68 / width 552.45` vs `left 898.12 / width 544.98`, with target left aligned at `378.92` vs `378.55`. By 650ms the current plane is nearly aligned (`left 355.36 / width 777.44` vs `left 353.02 / width 778.39`), and stable is effectively exact (`left 324.81 / width 790.47` on both sides).
- Confirmed the v95 plane easing change does not disturb Home-to-Detail entry. The refreshed v60 capture still reports close entry plane geometry at 120ms (`left 548.94 / width 360.23` reference vs `left 549.95 / width 340.72` local), 300ms (`left 390.02 / width 665.24` vs `left 393.09 / width 654.00`), 750ms (`left 330.30 / width 779.85` vs `left 330.14 / width 779.74`), and stable (`left 324.79 / width 790.42` local, matching the reference stable target). Title residual MAE remains low in the same v60 run: `0.0099` at 120ms, `0.0251` at 300ms, `0.0006` at 750ms, and `0.0001` stable.
- Verified v95 with `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, `node --check scripts/qa-detail-plane-offset-v27.mjs`, `node --check scripts/qa-plane-target-snap-v30.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-detail-switch-compare-v54.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-plane-offset-v27.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-meta-switch-v26.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-title-mode-move-v33.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-plane-target-snap-v30.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-detail-entry-compare-v60.mjs`, and `npm run build`.
- Recalibrated Home pixel-wheel travel from the original local `0.556` pixel factor to `HOME_WHEEL_PIXEL_FACTOR = 0.543`, while preserving the reference source's Firefox line-wheel branch as `HOME_WHEEL_LINE_FACTOR = 0.75`. This compensates for the local archive's 17 real photos being mapped onto the reference-style 30-slot visual rail, instead of blindly applying the source site's 30-project pixel step to a shorter photo set.
- The refreshed live/local v61 Home wheel comparison now reports stable visual travel reference/local `1.533` vs `1.540`, improving the previous `1.533` vs `1.570`; 80ms is `0.522` vs `0.520`, 160ms is `1.218` vs `1.200`, and 320ms is `1.443` vs `1.440`. Latency remains matched within about `0.002`, the stable pagination alpha box is still exact (`2700 / 137700`), and there are no runtime exceptions. The 80ms local active index can still sit on the rounding edge for one sample, but the focused Home wheel regression confirms the 160ms frame and settled state advance to `002 / 017` as expected.
- Confirmed the v96 wheel-step change does not disturb pointer dragging or keyboard target-lag. The refreshed live/local v76 Home drag comparison reports 200ms target/current index reference/local `0.533 / 0.259` vs `0.533 / 0.259`, settled `0.533 / 0.530` vs `0.533 / 0.530`, and latency deltas within `0.0002`. `scripts/qa-home-target-lag-v42.mjs` still keeps synchronous ArrowRight on `001` with target `630px`, and immediate Enter still opens `/nian-nian-001`.
- Verified v96 with `node --check src/main.js`, `node --check scripts/capture-home-wheel-compare-v61.mjs`, `node --check scripts/qa-home-wheel-v21.mjs`, `node --check scripts/qa-home-pagination-wave-v29.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-wheel-compare-v61.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-wheel-v21.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-pagination-wave-v29.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-drag-compare-v76.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-home-target-lag-v42.mjs`, and `npm run build`.
- Refreshed Work entry opacity evidence and updated `scripts/qa-work-entry-opacity-v52.mjs` to guard a live-reference early-fade band of `0.04..0.30` instead of the stale `0.04..0.16` band. The latest live v51 Work-entry capture reports reference 300ms active large-image opacity `0.202155`, while the local v52 sample reports `0.228182`, active large-layer top `346.37px`, stable opacity `1`, and no runtime exceptions. This keeps the test focused on "entered early fade but not settled" rather than overfitting one older decode-timing sample.
- Verified v97 with `node --check scripts/qa-work-entry-opacity-v52.mjs`, `node --check scripts/qa-work-layer-fx-v19.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-midtransition-compare-v51.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-entry-opacity-v52.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-work-layer-fx-v19.mjs`, and `npm run build`.
- Corrected the Work-entry comparison defaults so `scripts/capture-midtransition-compare-v51.mjs` and `scripts/qa-work-entry-opacity-v52.mjs` both compare the live reference first project against local `/nian-nian-001` by default. The previous local default `/nian-nian-002` created a false visual mismatch by comparing the reference `HOUSE OF GUCCI` slot to the local second photo/title.
- Expanded the focused v52 Work-entry early-fade band to `0.04..0.38` after repeated fresh live v51 reference captures ranged from about `0.20` to `0.33` at 300ms. The latest refreshed v51 run reports reference 300ms active large-image opacity `0.332137`; the focused local v52 run reports `0.228213`, active frame `top 314.65 / opacity 0.7`, active large-layer `top 359.31`, loaded `true`, stable opacity `1`, and no runtime exceptions.
- Matched the reference Work `VISIT SITE` control structure. Work now keeps the bottom CTA visible even when archive photos do not have external `visitUrl` values by falling back through `getWorkVisitUrl()` to the current Work image URL, preserving the reference-like visible affordance while still making the button useful for the private archive. The Work CTA visual now uses the reference two-line `↗` / `VISIT SITE` structure instead of the Detail Explore cross icon, and its geometry matches the live reference: refreshed v51 stable reports reference `top 788 / height 68 / bottom 856 / width 54.27` vs local `top 788 / height 68 / bottom 856 / width 54.45`. Crops: `/tmp/ref-v51-work-visit-crop.png` and `/tmp/local-v51-work-visit-crop.png`.
- Verified v98 with `node --check src/main.js`, `node --check scripts/qa-work-entry-opacity-v52.mjs`, `node --check scripts/capture-midtransition-compare-v51.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-midtransition-compare-v51.mjs`, local CDP runs for `scripts/qa-work-entry-v11.mjs`, `scripts/qa-work-entry-opacity-v52.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-keyboard-v12.mjs`, and `npm run build`.
- Added `scripts/qa-studio-v99.mjs` to guard the hidden upload backdoor without mutating the archive. The script verifies the static `/studio` route/docs/server upload wiring, the hidden `nian` key sequence, visible/interactable Studio panel state after fade-in, password key input, title-prefix input, multi-file input, initial `0 FILES` counter, and `/api/photos` manifest readability. The current run reports both secret-keyword and direct-route paths in `mode: "studio"`, visible `true`, `aria-hidden: "false"`, no `inert`, manifest `count: 17`, first `source-000001`, last `source-000017`, screenshots `/tmp/local-studio-v99-secret.png` and `/tmp/local-studio-v99-direct.png`, and no failures.
- Verified v99 with `node --check scripts/qa-studio-v99.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-studio-v99.mjs`, non-mutating local reads of `/studio`, `/api/photos`, and `/data/photos.json`, plus the existing `npm run build` pass from the v98 patch.
- Recalibrated Work internal media-switch motion with separate early lerps for large image layers and the active Work frame. `WORK_SWITCH_LAYER_LERP = 0.067` and `WORK_SWITCH_FRAME_LERP = 0.108` keep the first 120ms from outrunning the live reference, then both ease back toward the shared Work DOM lerp instead of using a hard frame hold. `WORK_SWITCH_REVEAL_DELAY` remains `180` so the 300ms image fade still enters the same live-reference band.
- The refreshed v100 live/local Work-switch comparison reports 120ms active-layer top reference/local `667.55` vs `668.33` (`+0.78px`), exiting-layer top `-163.14` vs `-162.13` (`+1.01px`), frame top `226.66` vs `224.84` (`-1.82px`), and normalized active-layer progress `0.4448` vs `0.4437`. At 300ms it reports active-layer top `339.38` vs `336.15` (`-3.23px`), exiting-layer top `-491.39` vs `-494.38` (`-2.99px`), frame top `265.87` vs `265.71` (`-0.16px`), image opacity `0.251423` vs `0.22817`, and stable active layer/frame alignment at `206.35px` / `271.5px` with opacity `1`.
- Verified v100 with `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/capture-work-switch-compare-v53.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-work-switch-compare-v53.mjs`, local CDP regressions for `scripts/qa-work-layer-fx-v19.mjs`, `scripts/qa-work-loaded-return-v22.mjs`, `scripts/qa-work-media-window-v32.mjs`, `scripts/qa-work-media-lazy-v48.mjs`, `scripts/qa-work-layer-noninvolved-v50.mjs`, `scripts/qa-work-entry-opacity-v52.mjs`, and `scripts/qa-work-keyboard-v12.mjs`, plus `npm run build`. Refreshed comparison crops: `/tmp/compare-v53-work-switch-120ms.jpg`, `/tmp/compare-v53-work-switch-300ms.jpg`, and `/tmp/compare-v53-work-switch-stable.jpg`.
- Recalibrated Detail project-switch early motion with `DETAIL_SWITCH_EARLY_SCROLL_EASE = 0.0495` and `DETAIL_SWITCH_EARLY_PLANE_EASE = 0.0495`. Repeated v54 sampling before this patch had a stable local over-advance at 120ms: active plane left reference/local around `882.45` vs `866.08` to `866.80` (`-15.67px` to `-16.37px`) and width `552.57` vs about `558.5` (`+5.7px` to `+6.0px`).
- The refreshed v101 normal-frame v54 capture reports 120ms active plane geometry at reference/local `left 882.70 / width 552.46` vs `left 882.09 / width 552.68`, for `-0.61px` left and `+0.22px` width residual. The 120ms target left is `378.92` vs `378.87` (`-0.05px`), 650ms remains close (`left -3.56px`, `width +1.52px`), stable remains effectively exact, and runtime exceptions are `0`. The live reference occasionally samples one frame faster at 120ms (`left` around `842-843px`); those runs were treated as reference-frame outliers rather than product regressions because local remained stable around `882-884px` and the next normal-frame capture matched.
- Verified v101 with `node --check src/main.js`, `node --check scripts/capture-detail-switch-compare-v54.mjs`, `node --check scripts/qa-detail-plane-offset-v27.mjs`, refreshed live/local `scripts/capture-detail-switch-compare-v54.mjs` summary/crops (`/tmp/compare-v54-detail-switch-120ms.jpg`), local CDP regressions for `scripts/qa-detail-plane-offset-v27.mjs`, `scripts/qa-detail-meta-switch-v26.mjs`, `scripts/qa-title-mode-move-v33.mjs`, and `scripts/qa-plane-target-snap-v30.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-detail-entry-compare-v60.mjs`, and `npm run build`. The refreshed v60 Home-to-Detail comparison keeps title residual MAE at `0.0192` (120ms), `0.0243` (300ms), `0.0014` (750ms), and `0.0001` stable; the known 300ms pgn-left residual remains the larger remaining entry artifact.
- Ported the reference `pgnX.show({ out: true })` direction split for Home-to-Detail pagination digits. The entering pgn now holds both halves at `-101%` until the show-out branch begins, then animates `pgn-a` from `+101%` toward `0` and `pgn-b` from `-101%` toward `0`, matching the reference's asymmetric split instead of the old same-direction entry. The show-out branch uses `showOutEnterDelay = 220` to preserve the reference-like hidden 120ms frame in local runtime.
- The refreshed v102 v60 Home-to-Detail comparison improves the 300ms pgn residual from the prior `a -6.5123px / b +1.3714px` to `a +0.5355px / b +0.0720px`. At 120ms both reference and local remain hidden-left (`pgnDelta a -0.1696px / b +2.5262px`, mostly `17` vs reference `30` text width), at 750ms pgn residual is `a +0.0063px / b +0.0182px`, and stable is exact `0 / 0`. The same run keeps title residual MAE at `0.0197` (120ms), `0.0243` (300ms), `0.0035` (750ms), and `0.0001` stable.
- Verified v102 with `node --check src/main.js`, `node --check scripts/qa-pgn-home-entry-v44.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-home-entry-v44.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-detail-entry-compare-v60.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-out-split-v39.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pgn-v15.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-pagination-canvas-v16.mjs`, and `npm run build`. Refreshed pgn comparison crops: `/tmp/compare-v60-home-detail-120ms.jpg`, `/tmp/compare-v60-home-detail-300ms.jpg`, `/tmp/compare-v60-home-detail-750ms.jpg`, and `/tmp/compare-v60-home-detail-stable.jpg`.
- Added a Detail/Work-to-Home exit geometry window with `DETAIL_EXIT_EARLY_PLANE_EASE = 0.058` for the first 360ms after leaving the project surface. This only affects the early WebGL plane chase when `state.mode === VIEW.index` after a Detail/Work exit; Home entry, Detail project switches, Work switches, and settled Home geometry keep their existing easing branches.
- The refreshed v103 v71 Detail-drag comparison now matches the reference's 260ms collapse frame: reference/local active plane `left 496.37 / width 447.27 / top 265.98 / height 368.03` vs `left 497.11 / width 445.79 / top 266.16 / height 367.67`. Before this patch, the same run had local `left 521.77 / width 396.46`, visibly too far along toward the Home strip. Settled remains close: reference/local `left 676.78 / width 86.43` vs `left 675.94 / width 88.11`.
- Verified v103 with `node --check src/main.js`, `node --check scripts/capture-detail-drag-compare-v71.mjs`, refreshed live/local `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-detail-drag-compare-v71.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-detail-drag-carry-v38.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-plane-target-snap-v30.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-wheel-compare-v61.mjs`, refreshed `SUMMARY_ONLY=1 CDP_URL=http://127.0.0.1:9239 node scripts/capture-home-drag-compare-v76.mjs`, and `npm run build`.
- Repaired `scripts/capture-control-hover-compare-v57.mjs` so its reference Detail entry uses the same robust Enter-or-center-click loop as the newer Home-to-Detail comparison. The old v57 entry path could time out on the live reference despite the reference site being healthy, producing a QA false negative before hover sampling began.
- Tightened the Detail Explore control geometry by anchoring `.gallery-shell.is-detail .visit-symbol` `3px` lower. The refreshed v57 live/local hover comparison now reports the Explore symbol at the same vertical box as the reference (`top 837 / bottom 851`), while preserving the project close symbol (`top 49 / bottom 63`), root heights, and SVG morph behavior.
- Unified older QA scripts from the stale default CDP port `9247` to the current `9239`, so loader/work/home/pgn legacy checks can run against the same Chrome session without one-off `CDP_URL` overrides.
- Verified v104 with `node --check scripts/capture-control-hover-compare-v57.mjs`, refreshed live/local `node scripts/capture-control-hover-compare-v57.mjs` (`failures: []`, runtime exceptions `0`), `node scripts/capture-mobile-fallback-compare-v66.mjs`, `node scripts/qa-studio-v99.mjs`, `node scripts/qa-loader-v14.mjs`, `CDP_URL=http://127.0.0.1:9239 node scripts/qa-loader-nav-v49.mjs`, `node scripts/qa-about-plane-source-v41.mjs`, `node scripts/qa-work-entry-opacity-v52.mjs`, repeated `SUMMARY_ONLY=1 node scripts/capture-work-switch-compare-v53.mjs`, `node scripts/qa-work-layer-fx-v19.mjs`, `node scripts/qa-home-wheel-v21.mjs`, and `npm run build`.
- Latest v104 Work switch evidence treated the first repeated v53 run as a reference-frame outlier (`120ms` layer delta around `71px`) because the immediate repeat returned to the normal live band: `120ms` active-layer delta `+2.66px`, exiting-layer delta `+2.87px`, frame delta `-2.02px`; `300ms` active-layer delta `+9.01px`, image opacity reference/local `0.227975 / 0.202285`; stable active layer delta `-0.01px` and opacity `1 / 1`.
- Updated `scripts/qa-home-detail-keyboard-v13.mjs` to match the current reference-style sparse Work media model. The script previously expected `ArrowRight` from `/nian-nian-002` Work to activate global Work index `2`, but the reference-pattern media window for that project is `1,3,4,5,7,9,10`, so the second media slot is global index `3`. The refreshed v13 run now reports `workAfterArrow.activeWorkIndex: "3"` and `workReturn.activeWorkIndex: "3"` with `failures: []`.
- Ran a broader v105 regression sweep across user paths, pagination, Work rail/media, shader/palette, and Home interaction. Passing local CDP checks: `qa-reference-hover-v20`, `qa-home-detail-keyboard-v13`, `qa-project-control-v24`, `qa-reference-dpr-v40`, `qa-pgn-v15`, `qa-pagination-canvas-v16`, `qa-pagination-scale-v25`, `qa-work-media-window-v32`, `qa-work-rail-v18`, `qa-work-media-order-direction-v34`, `qa-work-media-lazy-v48`, `qa-work-keyboard-v12`, `qa-work-loaded-return-v22`, `qa-work-thumb-reentry-v47`, `qa-work-layer-noninvolved-v50`, `qa-work-reentry-fx-v46`, `qa-shader-light-v28`, `qa-webgl-background-v64`, `qa-home-base-palette-v43`, `qa-home-target-lag-v42`, `qa-home-drag-edge-v45`, `qa-home-pagination-wave-v29`, `qa-work-color-v31`, `qa-work-active-frame-color-v35`, `qa-work-plane-photo-y-v37`, and `qa-work-rail-breakpoint-v36`.
- Refreshed v105 live reference/local comparisons after the regression sweep. `capture-home-detail-entry-compare-v60` still shows stable plane/pgn alignment (`stable` plane `324.79 / 790.42` local against reference `324.79 / 790.42`, pgn delta `0 / 0`), with 300ms title MAE `0.0233` and pgn delta `+1.2658px / -0.6226px`. `capture-detail-switch-compare-v54` reports the 120ms active plane at reference/local `left 882.45 / width 552.57` vs `left 883.98 / width 551.90`, 650ms `left 355.35 / width 777.45` vs `left 353.97 / width 778.03`, and stable alignment within `0.06px`. A repeated `capture-work-switch-compare-v53` run confirmed the normal live band after one reference-frame outlier: 120ms active layer delta `+3.67px`, exiting layer `+3.91px`, frame `-2.23px`; 300ms active layer `-1.54px`, frame `-0.27px`; stable active layer `-0.01px`.
- Verified v105 with `node --check scripts/qa-home-detail-keyboard-v13.mjs` and `npm run build`; the build still has only the known Vite chunk-size warning from the Three.js bundle.
- Added isolated storage overrides to the Node/Vite server (`GALLERY_DATA_DIR`, `GALLERY_MEDIA_DIR`, `GALLERY_UPLOAD_DIR`, `GALLERY_MANIFEST_PATH`) so the upload pipeline can be tested without mutating the real family-photo archive. The production defaults still point at `public/data`, `public/media`, and `.uploads/tmp`.
- Added `scripts/qa-upload-pipeline-v106.mjs` to exercise the hidden upload backdoor end to end with a synthetic 3000 x 2000 PNG, a random isolated server port, a wrong-key failure path, and a correct-key upload path.
- Verified v106 with `node --check server/index.ts`, `node --check server/photoPipeline.js`, `node --check scripts/qa-upload-pipeline-v106.mjs`, `node scripts/qa-upload-pipeline-v106.mjs`, a default `/api/photos` smoke check against the live local server, and `npm run build`. The latest v106 run reports wrong-key status `401`, correct-key status `200`, isolated manifest count `1`, metadata `title: QA Kid 001`, `width: 3000`, `height: 2000`, `aspect: 1.5`, generated WebP variants `thumb 520 x 347`, `medium 1280 x 853`, `large 2200 x 1467`, and empty upload temp directories after both the failed and successful upload attempts.
- Ran a final v107 guard pass with a WebGL-enabled headless Chrome. A first attempt with `--disable-gpu` produced a false Studio timeout because the app creates its `THREE.WebGLRenderer` before rendering the DOM shell; the valid QA browser keeps WebGL enabled.
- Verified v107 Studio and upload-adjacent behavior with `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-studio-v99.mjs`: secret `nian` entry and direct `/studio` route both land in `mode: studio`, the panel is visible and interactive, password/title/file controls are present, file counter starts at `0 FILES`, and `/api/photos` still reports 17 archive photos from `source-000001` through `source-000017`.
- Verified v107 local hard-assertion coverage with `node scripts/qa-home-detail-keyboard-v13.mjs`, `node scripts/qa-work-layer-fx-v19.mjs`, `node scripts/qa-work-loaded-return-v22.mjs`, `node scripts/qa-detail-drag-carry-v38.mjs`, `node scripts/qa-pgn-home-entry-v44.mjs`, `node scripts/qa-webgl-background-v64.mjs`, and `node scripts/capture-mobile-fallback-compare-v66.mjs`. These runs cover Home/Detail/Work keyboard paths, sparse Work media ordering, Work switch and return image loading, Detail drag exit carry, Home-to-Detail pagination structure, shader-backed WebGL background planes, and the mobile fallback with `/studio` still accessible.
- Refreshed v107 live reference/local comparisons after the guard pass. `capture-home-detail-entry-compare-v60` still reports exact stable pgn and plane alignment, title residual MAE `0.0360` at 300ms, `0.0029` at 750ms, and `0.0001` stable; `capture-detail-switch-compare-v54` still reports stable plane alignment within about `0.06px` and 650ms alignment around `0.4px`; `capture-work-switch-compare-v53` still reports stable Work large-layer alignment within `0.02px`. The 120ms Work and pgn comparison frames remain live-reference-sensitive and can swing by a single frame, so the focused local hard guards above are the stronger blocking signal for final local behavior.
- Corrected Work-switch QA timing in `scripts/qa-work-layer-fx-v19.mjs` and `scripts/qa-work-loaded-return-v22.mjs`: both scripts previously labeled an initial `180ms` sample as `120ms`. The first Work-switch sample now actually waits `120ms`, and `scripts/capture-work-switch-compare-v53.mjs` reports page-side `sampleMs` for reference/local samples so early-frame comparisons can be read against their real timing.
- Recalibrated Work internal switch motion after the true-120ms correction. `WORK_SWITCH_LAYER_LERP = 0.077` and `WORK_SWITCH_FRAME_LERP = 0.13` bring the Work large-image layer and active rail frame closer to the live reference's early switch frame without disturbing stable alignment.
- Verified v108 with `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-loaded-return-v22.mjs`, `node --check scripts/capture-work-switch-compare-v53.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-layer-fx-v19.mjs`, refreshed live/local `SUMMARY_ONLY=1 LOCAL_URL=http://127.0.0.1:5279/ node scripts/capture-work-switch-compare-v53.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-loaded-return-v22.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-rail-v18.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-keyboard-v12.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-media-window-v32.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-home-detail-keyboard-v13.mjs`, and `npm run build`.
- Latest v108 Work-switch comparison reports the true 120ms reference/local large-layer progress at `0.4648` vs `0.4563` (top delta `+6.85px`), active rail-frame progress `0.6474` vs `0.6442` (top delta `-0.38px`), 300ms-equivalent large-layer progress `0.8343` vs `0.8485`, and stable large-layer alignment within `0.06px`. The local hard guard reports true-120ms active layer `y = 452.72px`, 300ms active layer `y = 131.47px`, stable `y = 0.12px`, and no runtime exceptions.
- Added a `TIMING_ONLY=1` mode to `scripts/capture-work-switch-compare-v53.mjs` and page-side absolute waits to the Work QA scripts, so `120ms` and `300ms` samples are not shifted by screenshot capture time. This revealed that the previous Work image reveal delay was calibrated against delayed screenshot samples rather than the true 300ms frame.
- Recalibrated Work switch image reveal timing from `WORK_SWITCH_REVEAL_DELAY = 180` to `220`. The true 300ms image opacity now tracks the live reference early fade instead of becoming visibly too bright before the reference image enters.
- Verified v109 with `node --check src/main.js`, `node --check scripts/qa-work-layer-fx-v19.mjs`, `node --check scripts/qa-work-loaded-return-v22.mjs`, `node --check scripts/capture-work-switch-compare-v53.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-layer-fx-v19.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-loaded-return-v22.mjs`, `TIMING_ONLY=1 SUMMARY_ONLY=1 LOCAL_URL=http://127.0.0.1:5279/ node scripts/capture-work-switch-compare-v53.mjs`, and ordinary screenshot-mode `SUMMARY_ONLY=1 LOCAL_URL=http://127.0.0.1:5279/ node scripts/capture-work-switch-compare-v53.mjs`.
- Latest v109 Work-switch screenshot comparison reports 120ms reference/local large-layer progress `0.4879` vs `0.4770` (top delta `+8.87px`), active rail-frame progress `0.6725` vs `0.6670` (top delta `-0.64px`), true 300ms active image opacity `0.0746` vs `0.0616`, active-layer top delta `-1.35px`, and stable large-layer/image alignment at `0px`. The focused local guard reports sampleMs `128.1 / 302.9 / 1602.9`, active image opacity `0 / 0.03699 / 1`, and no runtime exceptions.
- Added `TIMING_ONLY=1` and page-side absolute waits to `scripts/capture-home-detail-entry-compare-v60.mjs`, mirroring the Work-switch timing harness so Home-to-Detail `120ms`, `300ms`, `750ms`, and stable samples are not shifted by screenshot capture time. The v60 harness now opens/closes its own CDP target, disables browser cache, reports `sampleMs`, and can produce metrics-only runs for early-frame tuning.
- Corrected the Home-to-Detail `.pgn` show-out pre-delay state. During the reference-style show-out branch, local now holds `pgn-a` outside the right crop at `+101%` and `pgn-b` outside the left crop at `-101%` before the `showOutEnterDelay = 220` entry easing begins. This matches the live reference's asymmetric split instead of keeping both halves hidden left.
- Updated `scripts/qa-pgn-home-entry-v44.mjs` source guards and 120ms assertions to protect the asymmetric Home-to-Detail pgn split. The focused local run reports Home with 30 root-visible pgn slots and one hidden active window, 120ms Detail entry with `aTransform: translate3d(101%, 0px, 0px)` and `bTransform: translate3d(-101%, 0px, 0px)`, stable Detail pgn settled at `0% / 0%`, and no failures.
- The refreshed v110 timing-only v60 comparison reports Home-to-Detail pgn residuals at 120ms `a +1.5656px / b +0.9757px`, 300ms `a +1.3916px / b -0.4695px`, 750ms `a +0.1026px / b -0.0342px`, and stable `0 / 0`, with no runtime exceptions. The ordinary screenshot-mode v60 run reports 120ms `a +0.8487px / b +1.7719px`, 300ms `a +2.5195px / b -1.5039px`, 750ms `a +0.1873px / b -0.1119px`, and stable `0 / 0`.
- Verified v110 with `node --check scripts/capture-home-detail-entry-compare-v60.mjs`, `node --check scripts/qa-pgn-home-entry-v44.mjs`, refreshed timing-only and screenshot-mode `scripts/capture-home-detail-entry-compare-v60.mjs`, `TARGET_URL=http://127.0.0.1:5279/ node scripts/qa-pgn-home-entry-v44.mjs`, `node scripts/qa-pgn-out-split-v39.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-home-detail-keyboard-v13.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-home-pagination-wave-v29.mjs`, and `npm run build`. The build still has only the known Vite chunk-size warning from the Three.js bundle.
- Ran a final v111 delivery guard before git staging. `node scripts/qa-upload-pipeline-v106.mjs` still reports the isolated upload flow as `ok: true`, wrong key `401`, correct key `200`, generated WebP variants `thumb 520x347`, `medium 1280x853`, `large 2200x1467`, and empty upload temp directories. `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-studio-v99.mjs` confirms both secret `nian` entry and direct `/studio` route open the Studio panel with key/title/file controls and the live local archive manifest still at 17 photos.
- The v111 browser delivery guard also passed `LOCAL_URL=http://127.0.0.1:5279/ node scripts/capture-mobile-fallback-compare-v66.mjs`, `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-webgl-background-v64.mjs`, and `LOCAL_URL=http://127.0.0.1:5279/ node scripts/qa-work-layer-fx-v19.mjs`. These cover the mobile fallback with Studio access, shader/WebGL background planes in Detail and Work, and the true-timed Work media switch samples (`sampleMs` about `128.7 / 303.5 / 1604.9`) with active image opacity `0 / 0.0372 / 1` and no runtime exceptions.
- Confirmed the git publish set with `git ls-files --others --exclude-standard`: it includes code, QA scripts, docs, package files, server code, and the two font files, while `.gitignore` excludes `public/media/`, `public/data/photos.json`, `dist/`, `.uploads/`, `node_modules/`, `.env`, and `.DS_Store`, so private generated photo assets and temporary upload originals are not committed.

- Migrated the runtime project to TypeScript without changing the intended visual behavior. The browser entry is now `src/main.ts`, Vite loads it directly from `index.html`, the Express/Vite upload server is `server/index.ts`, the image-processing helpers are `server/photoPipeline.ts`, and local photo sync runs through `scripts/sync-photos.ts`. The QA harness remains JavaScript `.mjs` tooling, with source guards updated to inspect the new TypeScript runtime files.
- Verified the TypeScript migration with `npm run typecheck`, `npm run build`, `node scripts/qa-upload-pipeline-v106.mjs`, production-preview CDP runs for `scripts/qa-studio-v99.mjs`, `scripts/qa-home-detail-keyboard-v13.mjs`, `scripts/qa-work-layer-fx-v19.mjs`, and `scripts/qa-pgn-home-entry-v44.mjs`, plus `node --check` across every `.mjs` QA script. The production-preview pgn run reports no runtime exceptions; the earlier dev-server pgn attempt was affected by an unrelated stale Vite HMR websocket port already in use.

current result: TypeScript migration ready to publish; private photo media remains local/ignored by git.
